    /**
     * <p>close</p>
     */
    @Override
    public void close() {
        if(m_connection != null) {
            try {
                m_connection.close();
            } catch (final SQLException e) {
                LOG.debug("unable to close JDBC connection", e);
            }
        }
    }

