    /**
     * @param defaultPort
     * @param defaultTimeout
     * @param defaultRetries
     */
    protected LdapDetector() {
        super(DEFAULT_PORT, 5000, 0);
        setServiceName("LDAP");
    }

