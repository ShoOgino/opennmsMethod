    public LineOrientedDecoder(Charset charset, String delimit) {
        m_charset = charset;
        if(delimit == null) {
            m_delimit = "\r\n";
        }else {
            m_delimit = delimit;
        }
    }

