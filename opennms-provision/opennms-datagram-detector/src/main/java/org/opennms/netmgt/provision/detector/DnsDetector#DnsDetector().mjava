    /**
     * @param defaultPort
     * @param defaultTimeout
     * @param defaultRetries
     */
    public DnsDetector() {
        super(DEFAULT_PORT, DEFAULT_TIMEOUT, DEFAULT_RETRY);
        
    }

