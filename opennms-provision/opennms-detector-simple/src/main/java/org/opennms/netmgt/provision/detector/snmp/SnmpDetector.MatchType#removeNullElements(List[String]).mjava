        private static List<String> removeNullElements(List<String> input) {
            return input
                    .stream()
                    .filter(v -> v != null)
                    .collect(Collectors.toList());
        }

