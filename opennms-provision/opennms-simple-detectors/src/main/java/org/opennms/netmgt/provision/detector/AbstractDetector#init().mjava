    public void init() {
        if (m_timeout <= 0) {
            throw new IllegalStateException(String.format("Timeout of %d is invalid.  Must be > 0", m_timeout));
        }
        
        onInit();
    }

