    public void onInit(){
        setTimeout(3000);
        setBanner("+OK");
        addResponseHandler(regexMatches("QUIT"), shutdownServer("+OK"));
        addErrorHandler(errorString("COMMAND NOT VALID"));        
    }

