    public T apply(T iface) {
        if (iface == null) {
            return null;
        }
        
        if (matches(iface)) {
            return act(iface);
        }
        
        return iface;
    }

