    public boolean startsWith(String prefix) {
        for(String line : m_responseList) {
           if(!line.startsWith(prefix)) {
               return false;
           }
        }
        return true;
    }

