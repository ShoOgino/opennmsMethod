    public void close() {
        LogUtils.infof(this, "Closing Socket");
        Socket socket = m_socket;
        m_socket = null;
        try {
            if (socket != null) {
                socket.close();
            }
        } catch (IOException e) {
        }       
    }

