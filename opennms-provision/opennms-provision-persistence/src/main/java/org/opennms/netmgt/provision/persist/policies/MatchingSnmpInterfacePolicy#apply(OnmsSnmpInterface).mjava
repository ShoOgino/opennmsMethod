    public OnmsSnmpInterface apply(OnmsSnmpInterface iface) {
        if (iface == null) {
            return null;
        }
        
        if (matches(iface)) {
            return act(iface);
        }
        
        return iface;
    }

