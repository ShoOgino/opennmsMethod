    /** {@inheritDoc} */
    @Override
    public OnmsNode act(final OnmsNode node) {
        if (getCategory() == null) {
            return node;
        }
        node.addRequisitionedCategory(getCategory());
        return node;

    }

