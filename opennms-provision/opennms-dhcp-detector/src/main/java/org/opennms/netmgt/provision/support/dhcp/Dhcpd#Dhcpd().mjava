    /**
     * Constructs a new DHCP server instance. All of the internal fields are
     * initialized to <code>null</code>.
     * 
     */
    private Dhcpd() {
    	super("OpenNMS.Dhcpd");
    	m_clients.clear();
        m_server = null;
        m_listener = null;
        m_worker = null;
    }

