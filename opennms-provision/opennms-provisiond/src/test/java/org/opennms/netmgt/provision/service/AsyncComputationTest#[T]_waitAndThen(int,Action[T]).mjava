    public <T> Action<T> waitAndThen(final int millis, final Action<T> action) {
        return new Action<T>() {
            public void action(T t) {
                sleep(millis);
                action.action(t);
            }
        };
    }

