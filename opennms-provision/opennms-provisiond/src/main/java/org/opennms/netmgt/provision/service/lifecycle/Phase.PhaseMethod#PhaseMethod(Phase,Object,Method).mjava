        public PhaseMethod(Phase phase, Object target, Method method) {
            super(phase.getCoordinator());
            m_phase = phase;
            m_target = target;
            m_method = method;
            add(phaseRunner());
        }

