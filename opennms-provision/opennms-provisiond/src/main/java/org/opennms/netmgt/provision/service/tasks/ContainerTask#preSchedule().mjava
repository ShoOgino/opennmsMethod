    @Override
    public void preSchedule() {
        m_triggerTask.schedule();
        synchronized (m_children) {
            for(Task task : m_children) {
                task.schedule();
            }
            m_children.clear();
        }
    }

