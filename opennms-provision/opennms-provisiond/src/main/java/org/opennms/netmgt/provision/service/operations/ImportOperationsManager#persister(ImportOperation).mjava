    @SuppressWarnings("unused")
    private Runnable persister(final ImportOperation oper) {
        Runnable r = new Runnable() {
                @Override
        	public void run() {
        		oper.persist();
        	}
        };
        return r;
    }

