    private void add(BaseTask task) {
        super.addPrerequisite(task);
        addChildDependencies(task);
        m_children.add(task);
        if (isScheduled()) {
            task.schedule();
        }
    }

