    protected B doCompute(A a) throws Exception {
        try {
            B ret = m_computation.compute(a);
            complete(ret);
            return ret;
        } finally {
            m_waitFor.countDown();
        }
    }

