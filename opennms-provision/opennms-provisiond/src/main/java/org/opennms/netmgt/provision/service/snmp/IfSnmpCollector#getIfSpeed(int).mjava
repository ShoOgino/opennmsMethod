    public Long getIfSpeed(final int ifIndex) {
        Long ifSpeed = null;
        
        if (hasIfTable()) {
            ifSpeed = m_ifTable.getIfSpeed(ifIndex);
        }
        return ifSpeed;
    }

