    @SuppressWarnings("unused")
    private Runnable scanner(final ImportOperation oper) {
        return new Runnable() {
            @Override
            public void run() {
                log().info("Preprocess: "+oper);
                oper.scan();
            }
        };
    }

