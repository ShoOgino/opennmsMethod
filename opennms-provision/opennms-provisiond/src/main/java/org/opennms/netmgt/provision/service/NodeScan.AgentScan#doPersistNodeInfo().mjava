        public void doPersistNodeInfo() {
            if (m_node == null) {
                return;
            }
            m_nodeId = m_provisionService.updateNodeAttributes(getNode()).getId();
        }

