    /**
     * <p>scan</p>
     */
    @Override
    public void scan() {
        if (m_rescanExisting) {
    	updateSnmpData();
	} else {
            LOG.debug("Skipping scan for node {}: rescanExisting is false", getNode());
	}
    }

