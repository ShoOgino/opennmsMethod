    /**
     * <p>scan</p>
     */
    public void scan() {
        if (m_rescanExisting) {
    	updateSnmpData();
	} else {
            LogUtils.debugf(this, "Skipping scan for node %s: rescanExisting is false", getNode());
	}
    }

