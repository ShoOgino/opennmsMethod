    public Runnable scheduler(final BaseTask task) {
        return new Runnable() {
            public void run() {
                if (!hasBefores(task)) {
                    submit(task);
                } 
            }
        };
    }

