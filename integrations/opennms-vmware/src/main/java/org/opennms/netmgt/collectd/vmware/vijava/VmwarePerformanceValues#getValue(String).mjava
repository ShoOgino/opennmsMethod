    public Long getValue(String name) {
        Object object = values.get(name);

        if (object instanceof Long) {
            return (Long) object;
        } else {
            return null;
        }
    }

