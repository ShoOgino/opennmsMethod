        public double eval(double x) {
            double sum = 0;
            int k = 0;
            for (Double coeff : m_coeffs) {
                sum += coeff * Math.pow(x, k++);
            }
            return sum;
        }

