    /**
     * <p>hasStandbyUrl</p>
     *
     * @return a boolean.
     */
    @Override
    public boolean hasStandbyUrl() {
        getReadLock().lock();
        try {
            return (m_config.getStandbyUrlCount() > 0);
        } finally {
            getReadLock().unlock();
        }
    }

