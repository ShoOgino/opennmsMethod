    /**
     * <p>hasStandbyUrl</p>
     *
     * @return a boolean.
     */
    @Override
    public boolean hasStandbyUrl() {
        try {
            getReadLock().lock();
            return (m_config.getStandbyUrls().size() > 0);
        } finally {
            getReadLock().unlock();
        }
    }

