    /**
     * <p>setStatus</p>
     *
     * @param status a int.
     */
    protected void setStatus(int status) {
        synchronized (m_statusLock) {
            m_status = status;
            m_statusLock.notifyAll();
        }
    }

