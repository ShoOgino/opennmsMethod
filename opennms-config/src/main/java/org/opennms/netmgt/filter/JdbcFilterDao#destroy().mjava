    @PreDestroy
    @Transactional(propagation = Propagation.NOT_SUPPORTED)
    public void destroy() throws Exception {
        if (jmxReporter != null) {
            jmxReporter.stop();
            jmxReporter = null;
        }
    }

