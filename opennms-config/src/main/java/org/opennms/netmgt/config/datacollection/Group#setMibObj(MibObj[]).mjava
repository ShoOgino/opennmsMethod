    /**
     * 
     * 
     * @param mibObjs
     */
    public void setMibObj(final MibObj[] mibObjs) {
        m_mibObjects.clear();
        for (int i = 0; i < mibObjs.length; i++) {
                m_mibObjects.add(mibObjs[i]);
        }
    }

