    /**
     * Return the port on which eventd listens for UDP data.
     *
     * @return the port on which eventd listens for UDP data
     */
    public int getUDPPort() {
        try {
            getReadLock().lock();
            return m_config.getUDPPort();
        } finally {
            getReadLock().unlock();
        }
    }

