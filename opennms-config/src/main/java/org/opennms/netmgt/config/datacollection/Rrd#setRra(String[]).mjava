    /**
     * 
     * 
     * @param rras
     */
    public void setRra(final String[] rras) {
        m_rras.clear();
        for (int i = 0; i < rras.length; i++) {
                m_rras.add(rras[i].intern());
        }
    }

