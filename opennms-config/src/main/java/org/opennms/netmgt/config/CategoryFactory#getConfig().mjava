    /**
     * Return the categories configuration.
     *
     * @return the categories configuration
     */
    @Override
    public Catinfo getConfig() {
        try {
            getReadLock().lock();
            return m_config;
        } finally {
            getReadLock().unlock();
        }
    }

