    /**
     * Return the categories configuration.
     *
     * @return the categories configuration
     */
    @Override
    public Catinfo getConfig() {
        getReadLock().lock();
        try {
            return m_config;
        } finally {
            getReadLock().unlock();
        }
    }

