    /**
     * <p>getConfig</p>
     *
     * @return Returns the config.
     */
    protected Outages getObject() {
        getReadLock().lock();
        try {
            return getContainer().getObject();
        } finally {
            getReadLock().unlock();
        }
    }

