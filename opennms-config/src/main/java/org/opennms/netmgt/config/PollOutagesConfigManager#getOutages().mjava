    /**
     * Return the outages configured.
     *
     * @return the outages configured
     */
    public List<Outage> getOutages() {
        getReadLock().lock();
        try {
            return getObject().getOutages();
        } finally {
            getReadLock().unlock();
        }
    }

