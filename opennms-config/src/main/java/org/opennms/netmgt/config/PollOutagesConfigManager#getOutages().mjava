    /**
     * Return the outages configured.
     *
     * @return the outages configured
     */
    public Outage[] getOutages() {
        getReadLock().lock();
        try {
            return getObject().getOutage();
        } finally {
            getReadLock().unlock();
        }
    }

