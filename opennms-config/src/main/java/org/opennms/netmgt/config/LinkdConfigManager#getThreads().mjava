    /**
     * <p>getThreads</p>
     *
     * @return a int.
     */
    @Override
    public int getThreads() {
        if (m_config.hasThreads()) return m_config.getThreads();
        return 5;
    }

