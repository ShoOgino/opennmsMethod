        public DatabaseReportPage open() {
            LOG.debug("Opening page '{}'", url);
            driver.get(url);
            pageContainsText("Early morning report");
            return this;
        }

