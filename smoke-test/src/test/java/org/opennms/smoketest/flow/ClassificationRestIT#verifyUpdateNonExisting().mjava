    @Test
    public void verifyUpdateNonExisting() {
        given().contentType(ContentType.JSON)
                .body("{}")
                .put("1000")
                .then().assertThat()
                .statusCode(404);
    }

