        public ReportTemplateTab open() {
            LOG.debug("Open Report Template Tab");
            getElement().click();
            assertThat(isActive(), Matchers.is(true));
            return this;
        }

