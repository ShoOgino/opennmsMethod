    @Test
    public void verifyWhoami() {
        given().accept(ContentType.JSON).get()
                .then().log().all()
                .assertThat().statusCode(200)
                .body("user-id", is("admin"))
                .body("full-name", is("Administrator"))
                .body("role", hasItem("ROLE_ADMIN"));
    }

