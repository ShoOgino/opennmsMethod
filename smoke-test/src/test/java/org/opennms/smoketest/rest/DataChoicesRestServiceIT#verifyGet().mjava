    @Test
    public void verifyGet() {
        given()
            .queryParam("action", "disable")
        .when()
            .get("/rest/datachoices")
                .then()
                .statusCode(200);
    }

