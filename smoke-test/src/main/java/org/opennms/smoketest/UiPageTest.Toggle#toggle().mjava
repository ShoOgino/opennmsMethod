        public void toggle() {
            boolean previousState = isOn();
            execute(() -> m_driver.findElement(By.id(id))).click();
            new WebDriverWait(m_driver, 5, 500).until((Function<WebDriver, Boolean>) webDriver -> previousState != isOn());
        }

