    protected boolean hasRow() {
        if (isFinished()) {
            for (Queue<SnmpResult> q : m_pendingData) {
                if (!q.isEmpty()) {
                    return true;
                }
            }
            return false;
        } else {
            for (Queue<SnmpResult> q : m_pendingData) {
                if (q.isEmpty()) {
                    return false;
                }
            }
            return true;
        }
    }

