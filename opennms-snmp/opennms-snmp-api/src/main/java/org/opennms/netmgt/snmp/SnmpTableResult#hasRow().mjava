    boolean hasRow() {
        if (isFinished()) {
            if (!getPendingData().isEmpty()) {
                return true;
            }
            return false;
        } else {
            for (SnmpRowResult rr : getPendingData().values()) {
                if (rr.isComplete()) {
                    return true;
                }
            }
            return false;
        }
    }

