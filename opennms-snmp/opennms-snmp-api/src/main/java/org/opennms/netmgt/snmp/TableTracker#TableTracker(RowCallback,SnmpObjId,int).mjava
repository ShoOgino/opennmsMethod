    public TableTracker(RowCallback rc, SnmpObjId base, int maxRepititions) {
        m_rowCallback = rc;
        m_base = base;
        m_last = base;
        m_maxRepetitions = maxRepititions; 
    }

