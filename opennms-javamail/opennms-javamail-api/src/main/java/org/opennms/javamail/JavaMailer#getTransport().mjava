    /**
     * <p>getTransport</p>
     *
     * @return a {@link java.lang.String} object.
     */
    public String getTransport() {
        if (isUseJMTA()) {
            return "mta";
        } else {
            return m_transport;
        }
    }

