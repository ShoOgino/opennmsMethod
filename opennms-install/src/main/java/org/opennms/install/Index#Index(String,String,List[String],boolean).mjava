    public Index(String name, String table, List<String> columns,
            boolean unique) {
        m_name = name;
        m_table = table;
        m_columns = columns;
        m_unique = unique;
    }

