    public Index(String name, String table, String using, List<String> columns,
            boolean unique) {
        m_name = name;
        m_table = table;
        m_using = using;
        m_columns = columns;
        m_unique = unique;
    }

