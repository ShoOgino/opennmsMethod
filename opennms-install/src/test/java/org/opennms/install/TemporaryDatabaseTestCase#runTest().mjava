    @Override
    protected void runTest() throws Throwable {
        if (!areTestsEnabled()) {
            return;
        }

        try {
            super.runTest();
        } catch (Throwable t) {
            setTestFailureThrowable(t);
            throw t;
        }
    }

