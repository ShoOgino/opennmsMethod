    @Override
    protected void runTest() throws Throwable {
        if (!areTestsEnabled()) {
            return;
        }

        try {
            super.runTest();
        } catch (Throwable t) {
            m_failure = true;
            m_throwable = t;
            throw t;
        }
    }

