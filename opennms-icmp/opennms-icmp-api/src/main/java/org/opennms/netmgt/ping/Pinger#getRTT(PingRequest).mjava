    private Long getRTT(PingRequest reply) {
        if (reply.isSignaled()) {
        	ICMPEchoPacket p = reply.getPacket();
        	if (p != null) {
                return p.getPingRTT();
        	}
        }
        return null;
    }

