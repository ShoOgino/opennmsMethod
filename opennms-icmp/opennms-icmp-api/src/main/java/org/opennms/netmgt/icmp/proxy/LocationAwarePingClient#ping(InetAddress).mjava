    /**
     * Pings the given address.
     *
     * @param inetAddress The address to ping.
     * @return A request builder to provide additional request options.
     */
    PingRequestBuilder ping(InetAddress inetAddress);

