    /** {@inheritDoc} */
    @Override
    public void processError(Throwable t) {
        try {
            m_callback.handleError(m_id.getAddress(), m_request, t);
        } finally {
            setProcessed(true);
        }
    }

