    @Override
    public int hashCode() {
        int hash = 1;
        hash = hash * 31 + m_addr.hashCode();
        hash = hash * 31 + (int)(m_tid >>> 32);
        hash = hash * 31 + (int)(m_tid);
        hash = hash * 31 + m_seqId;
        return hash;
    }

