    public Jni6PingRequest(final Jni6PingRequestId id, final long timeout, final int retries, final ThreadCategory log, final PingResponseCallback callback) {
        m_id = id;
        m_timeout = timeout;
        m_retries = retries;
        m_log = log;
        m_callback = callback;
    }

