    /** {@inheritDoc} */
    public void processError(Throwable t) {
        try {
            m_callback.handleError(getAddress(), getRequest(), t);
        } finally {
            setProcessed(true);
        }
    }

