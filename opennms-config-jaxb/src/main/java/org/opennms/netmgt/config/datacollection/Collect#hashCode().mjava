    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((m_includeGroups == null) ? 0 : m_includeGroups.hashCode());
        return result;
    }

