    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((m_template == null) ? 0 : m_template.hashCode());
        return result;
    }

