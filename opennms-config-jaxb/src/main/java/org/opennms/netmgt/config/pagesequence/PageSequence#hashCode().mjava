    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((m_pages == null) ? 0 : m_pages.hashCode());
        return result;
    }

