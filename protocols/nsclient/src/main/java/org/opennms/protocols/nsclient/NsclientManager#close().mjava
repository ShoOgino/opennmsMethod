    /**
     * Closes the socket.
     */
    public void close() {
        try {
            m_Socket.close();
        } catch (final Exception e) {
            LOG.debug("unable to close socket", e);
        }
    }

