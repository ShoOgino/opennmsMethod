    /**
     * Creates the loaded log message.
     *
     * @param translatedConfig the translated configuration
     * @param diffTime the diff time
     * @return the string
     */
    protected String createLoadedLogMessage(V translatedConfig, long diffTime) {
        return "Loaded " + getDescription() + " in " + diffTime + "ms";
    }

