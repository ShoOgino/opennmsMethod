    /**
     * Gets the integer representation of the SNMP version.
     *
     * @return the SNMP version as integer
     */
    public int intValue() {
        if (isV1())
            return 1;
        if (isV2())
            return 2;
        if (isV3())
            return 3;
        return 0;
    }

