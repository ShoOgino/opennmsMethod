    public void tick() {
        firing.set(true);
        lock.lock();
        try {
            kieSession.fireAllRules();
        } finally {
            lock.unlock();
            firing.set(false);
        }
    }

