    public synchronized void stop() {
        if (timer != null) {
            timer.cancel();
        }
        if (kieSession != null) {
            kieSession.halt();
            kieSession = null;
        }
        if (kieContainer != null) {
            kieContainer.dispose();
            kieContainer = null;
        }
        started = false;
    }

