    private void unScheduleReads() {
        if (m_timer != null) {
            m_status = FINISHING;
            m_timer.cancel();
            m_timer = null;
        } else {
            //TODO: log something
        }
    }

