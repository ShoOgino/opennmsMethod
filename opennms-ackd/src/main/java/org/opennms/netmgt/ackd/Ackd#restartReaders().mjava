    protected void restartReaders() throws Exception {
        log().info("restartReaders: restarting readers...");
        stopReaders();
        startReaders();
        log().info("restartReaders: readers restarted.");
        
    }

