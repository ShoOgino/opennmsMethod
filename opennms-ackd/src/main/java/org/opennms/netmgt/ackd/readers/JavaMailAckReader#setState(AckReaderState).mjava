    /**
     * Anything calling this method should already have the lock.
     * 
     * @param state
     */
    private void setState(AckReaderState state) {
        synchronized (m_lock) {
            m_state = state;
        }
    }

