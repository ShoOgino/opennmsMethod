    /**
     * <p>add</p>
     *
     * @param o a E object.
     * @return a boolean.
     */
    public boolean add(E o) {
        if (contains(o)) {
            return false;
        }
        m_added.add(o);
        return true;
    }

