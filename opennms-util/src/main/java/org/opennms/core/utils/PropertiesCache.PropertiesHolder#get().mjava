        public Properties get() throws IOException {
            r.lock();
            try {
                if (m_properties == null) {
                    readWithDefault(new Properties());
                }
                return m_properties;
            } finally {
                r.unlock();
            }
        }

