        public Properties find() throws IOException {
            r.lock();
            try {
                if (m_properties == null) {
                    readWithDefault(null);
                }
                return m_properties;
            } finally {
                r.unlock();
            }
        }

