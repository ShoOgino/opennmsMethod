    /**
     * <p>set</p>
     *
     * @param index a int.
     * @param element a E object.
     * @return a E object.
     */
    public E set(int index, E element) {
        load();
        return m_list.set(index, element);
    }

