    /**
     * <p>add</p>
     *
     * @param index a int.
     * @param element a E object.
     */
    public void add(int index, E element) {
        load();
        m_list.add(index, element);
    }

