    /**
     * <p>remove</p>
     *
     * @param index a int.
     * @return a E object.
     */
    @Override
    public E remove(int index) {
        load();
        return m_list.remove(index);
    }

