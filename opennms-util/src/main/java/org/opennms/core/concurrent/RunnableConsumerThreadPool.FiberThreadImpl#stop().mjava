        /**
         * Sets the stop flag in the thread.
         */
        @Override
        public void stop() {
            m_status = STOP_PENDING;
            m_shutdown = true;
            // m_delegateThread.interrupt();
        }

