        /**
         * Returns true if the queue is currently open.
         * 
         * @return True if the queue is open.
         */
        @Override
        public boolean isOpen() {
            return !m_isClosed;
        }

