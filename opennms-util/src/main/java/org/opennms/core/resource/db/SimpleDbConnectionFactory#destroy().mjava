    /**
     * Clear all database URL and credential information so no more connections
     * can be requested.
     */
    @Override
    public void destroy() {
        this.url = null;
        this.username = null;
        this.password = null;
    }

