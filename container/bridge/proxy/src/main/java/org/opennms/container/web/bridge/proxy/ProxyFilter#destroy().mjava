    @Override
    public void destroy() {
        servletTracker.close();
        resourceTracker.close();
        handlerRwLock.writeLock().lock();
        try {
            handlers.clear();
        } finally {
            handlerRwLock.writeLock().unlock();
        }
    }

