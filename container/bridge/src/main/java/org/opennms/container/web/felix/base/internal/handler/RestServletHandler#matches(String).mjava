    @Override
    public boolean matches(String uri) {
        boolean matches = super.matches(uri);
        if (matches) {
            return !getEndpoints(uri).isEmpty();
        }
        return matches;
    }

