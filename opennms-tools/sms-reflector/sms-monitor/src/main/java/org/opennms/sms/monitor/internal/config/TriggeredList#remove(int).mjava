    /** {@inheritDoc} */
    @Override
    public T remove(int index) {
        T element = m_backingList.remove(index);
        if (element != null) {
            onRemove(index, element);
        }
        return element;
    }

