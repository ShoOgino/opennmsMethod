    /** {@inheritDoc} */
    @Override
    public void add(int index, T element) {
        m_backingList.add(index, element);
        if (element != null) {
            onAdd(index, element);
        }
    }

