        private void appendFieldPrefix(String name) {
            if (!m_first) {
                buf.append(", ");
            } else {
                m_first = false;
            }
            buf.append(name);
            buf.append(" = ");
        }

