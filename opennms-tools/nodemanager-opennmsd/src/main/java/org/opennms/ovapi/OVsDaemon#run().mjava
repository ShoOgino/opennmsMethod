	public void run() {
	    
	    ovspmd.OVsInit();
	    String response = onInit();
	    ovspmd.OVsInitComplete(response);
	    
	    int cmd = ovspmd.OVsReceive();
	    while (cmd != OVsPMD.OVS_CMD_EXIT) {
	        
	    }
	    
	    String stopResponse = onStop();
	    
	    ovspmd.OVsDone(stopResponse);
	    

	}

