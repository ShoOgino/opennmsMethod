    private String checkForCommandsAndTrim(String str) {
        if(str.contains("--")) {
            String[] splitStr = str.split("--");
            return splitStr[0];
        }
        
        return str;
    }

