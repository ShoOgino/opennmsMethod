    /**
     * Normalize.
     *
     * @throws Exception the exception
     */
    public void normalize() throws Exception {
        for (Source source : configSources) {
            unmarshall(source);
        }
    }

