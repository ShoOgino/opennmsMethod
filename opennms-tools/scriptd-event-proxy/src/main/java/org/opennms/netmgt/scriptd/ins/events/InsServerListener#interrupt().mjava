	@Override
	/**
	 * Stops the listener
	 */
	public void interrupt() {
        Category log = getLog();
		log.info("InsServerListener Interrupted!");
		try {
			listener.close();
		} catch (IOException e) {
			e.printStackTrace();
		}
		super.interrupt();
	}

