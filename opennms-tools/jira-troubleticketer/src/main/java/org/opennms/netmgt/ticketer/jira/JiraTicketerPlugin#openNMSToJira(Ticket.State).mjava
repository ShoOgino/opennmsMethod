    private int openNMSToJira(Ticket.State state) {

        switch (state) {

            case OPEN:
                return 1;
            case CANCELLED:
                return 10033;
            case CLOSED:
                //Resolved
                return 5;
            default:
                return 1;

        }
    }

