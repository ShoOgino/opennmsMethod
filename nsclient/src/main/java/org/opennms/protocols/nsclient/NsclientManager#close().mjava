    /**
     * Closes the socket.
     */
    public void close() {
        try {
            m_Socket.close();
        } catch (final Exception e) {
            LogUtils.debugf(this, e, "unable to close socket");
        }
    }

