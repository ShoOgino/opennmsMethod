    DashboardLayout() {
    
        m_base = new VerticalPanel();
        
        Label label =new Label("dashboard");
        
        m_base.add(label);
        
        initWidget(m_base);
    }

