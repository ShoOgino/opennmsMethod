    public void setFilters(final List<Filter> filters) {
        if (filters == m_filters) return;
        if (filters == null) {
            m_filters = null;
        } else {
            m_filters.clear();
            m_filters.addAll(filters);
        }
    }

