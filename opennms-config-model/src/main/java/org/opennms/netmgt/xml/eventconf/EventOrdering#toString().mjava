    @Override
    public String toString() {
        return MoreObjects.toStringHelper(this)
                .add("parent", m_parent)
                .add("idx", m_sequenceIndex)
                .toString();
    }

