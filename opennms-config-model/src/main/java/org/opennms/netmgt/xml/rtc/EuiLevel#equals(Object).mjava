    @Override
    public boolean equals(final Object other) {
        if (!(other instanceof EuiLevel)) {
            return false;
        }
        EuiLevel castOther = (EuiLevel) other;
        return Objects.equals(header, castOther.header) && Objects.equals(category, castOther.category);
    }

