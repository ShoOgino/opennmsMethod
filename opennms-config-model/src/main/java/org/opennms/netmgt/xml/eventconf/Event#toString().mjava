    @Override
    public String toString() {
        return MoreObjects.toStringHelper(this)
                .add("uei", m_uei)
                .add("priority", getPriority())
                .add("mask", m_mask)
                .toString();
    }

