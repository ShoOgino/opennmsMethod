    @Override
    public boolean equals(final Object obj) {
        if ( this == obj ) {
            return true;
        }
        
        if (obj instanceof Roles) {
            final Roles temp = (Roles)obj;
            return Objects.equals(temp.m_roles, m_roles);
        }
        return false;
    }

