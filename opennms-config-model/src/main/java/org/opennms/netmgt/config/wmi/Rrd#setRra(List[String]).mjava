    public void setRra(final List<String> rras) {
        if (rras == m_rras) return;
        m_rras.clear();
        if (rras != null) {
            for (final String rra : rras) {
                addRra(rra);
            }
        }
    }

