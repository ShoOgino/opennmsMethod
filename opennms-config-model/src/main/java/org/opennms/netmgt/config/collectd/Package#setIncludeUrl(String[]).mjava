    /**
     * 
     * 
     * @param urls
     */
    public void setIncludeUrl(final String[] urls) {
        m_includeUrls.clear();
        for (final String url : urls) {
            m_includeUrls.add(url);
        }
    }

