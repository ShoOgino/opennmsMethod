    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            relaxed, 
            description, 
            type, 
            dsType, 
            value, 
            rearm, 
            trigger, 
            dsLabel, 
            triggeredUEI, 
            rearmedUEI, 
            filterOperator, 
            resourceFilterList);
        return hash;
    }

