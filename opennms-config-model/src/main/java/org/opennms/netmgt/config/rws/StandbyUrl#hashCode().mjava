    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            server_url, 
            timeout, 
            directory, 
            username, 
            password);
        return hash;
    }

