    /**
     * 
     * 
     * @param packages
     */
    public void setPackage(final Package[] packages) {
        m_packages.clear();
        for (final Package p : packages) {
            m_packages.add(p);
        }
    }

