    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            listenAddress, 
            syslogPort, 
            newSuspectOnMessage, 
            forwardingRegexp, 
            matchingGroupHost, 
            matchingGroupMessage, 
            parser, 
            discardUei, 
            threads, 
            queueSize, 
            batchSize, 
            batchInterval);
        return hash;
    }

