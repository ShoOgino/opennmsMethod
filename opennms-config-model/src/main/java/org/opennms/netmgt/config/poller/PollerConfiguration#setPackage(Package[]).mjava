    /**
     * 
     * 
     * @param packages
     */
    public void setPackage(final Package[] packages) {
        m_packages.clear();
        for (final Package pack : packages) {
            m_packages.add(pack);
        }
    }

