    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            binary, 
            name, 
            execute, 
            comment, 
            contactType, 
            argumentList);
        return hash;
    }

