    @Override
    public int hashCode() {
        return Objects.hash(m_engines, 
            m_startScripts, 
            m_stopScripts, 
            m_reloadScripts, 
            m_eventScripts,
            m_transactional);
    }

