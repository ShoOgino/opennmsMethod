    public void setTimes(final List<Time> times) {
        ConfigUtils.assertMinimumSize(times, 1, "times");
        if (times == m_times) return;
        m_times.clear();
        if (times != null) m_times.addAll(times);
    }

