    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            delay, 
            retries, 
            useCategories, 
            defaultType, 
            policies, 
            mappingList);
        return hash;
    }

