    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            threads, 
            initial_sleep_time, 
            rescan_interval, 
            max_bft, 
            useCdpDiscovery, 
            useBridgeDiscovery, 
            useLldpDiscovery, 
            useOspfDiscovery, 
            useIsisDiscovery);
        return hash;
    }

