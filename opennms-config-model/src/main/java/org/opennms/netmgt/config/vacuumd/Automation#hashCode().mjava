    @Override
    public int hashCode() {
        return Objects.hash(m_name,
                            m_interval,
                            m_triggerName,
                            m_actionName,
                            m_autoEventName,
                            m_actionEvent,
                            m_active);
    }

