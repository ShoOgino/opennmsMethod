    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            name, 
            rrdRepository, 
            thresholdList, 
            expressionList);
        return hash;
    }

