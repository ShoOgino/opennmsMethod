    @Override
    public boolean equals(final Object obj) {
        if ( this == obj ) {
            return true;
        }

        if (obj instanceof WsManAssetAdapterConfiguration) {
            final WsManAssetAdapterConfiguration that = (WsManAssetAdapterConfiguration)obj;
            return Objects.equals(this.m_packages, that.m_packages);
        }
        return false;
    }

