    /**
     * 
     * 
     * @param times
     */
    public void setTime(final Time[] times) {
        m_times.clear();
        for (final Time time : times) {
            m_times.add(time);
        }
    }

