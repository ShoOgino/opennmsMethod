    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            criteria, 
            name, 
            interval, 
            userDefined, 
            status, 
            port, 
            retry, 
            timeout, 
            maxVarsPerPdu, 
            maxInterfacePerPdu);
        return hash;
    }

