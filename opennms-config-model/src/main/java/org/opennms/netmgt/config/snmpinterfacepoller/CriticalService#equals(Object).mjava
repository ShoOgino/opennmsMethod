    @Override
    public boolean equals(final Object obj) {
        if ( this == obj ) {
            return true;
        }

        if (obj instanceof CriticalService) {
            final CriticalService that = (CriticalService)obj;
            return Objects.equals(this.m_name, that.m_name);
        }
        return false;
    }

