    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            retry, 
            timeout, 
            username, 
            domain, 
            password, 
            rangeList, 
            specificList, 
            ipMatchList);
        return hash;
    }

