    @Override
    public boolean equals(final Object obj) {
        if ( this == obj ) {
            return true;
        }

        if (obj instanceof HostaddrMatch) {
            final HostaddrMatch that = (HostaddrMatch)obj;
            return Objects.equals(this.m_expression, that.m_expression);
        }
        return false;
    }

