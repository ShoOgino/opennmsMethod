    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            configuration, 
            ueiList, 
            hideMessage, 
            importFileList);
        return hash;
    }

