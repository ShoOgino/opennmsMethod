    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            host, 
            port, 
            userid, 
            password, 
            tl1ClientApi, 
            tl1MessageParser, 
            reconnectDelay);
        return hash;
    }

