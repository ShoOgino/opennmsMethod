    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            name, 
            delay, 
            retries, 
            useCategories, 
            defaultType, 
            _package, 
            scheduleList);
        return hash;
    }

