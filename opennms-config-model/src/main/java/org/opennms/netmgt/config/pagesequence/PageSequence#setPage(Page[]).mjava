    /**
     * 
     * 
     * @param pages
     */
    public void setPage(final Page[] pages) {
        m_pages.clear();
        for (final Page page : pages) {
            m_pages.add(page);
        }
    }

