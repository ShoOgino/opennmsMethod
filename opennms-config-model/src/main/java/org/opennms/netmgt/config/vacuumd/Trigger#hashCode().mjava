    @Override
    public int hashCode() {
        return Objects.hash(m_name,
                            m_dataSource,
                            m_operator,
                            m_rowCount,
                            m_statement);
    }

