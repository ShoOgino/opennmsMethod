    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            interval, 
            threads, 
            service, 
            suppressAdminDownEvent, 
            useCriteriaFilters, 
            nodeOutage, 
            _packageList);
        return hash;
    }

