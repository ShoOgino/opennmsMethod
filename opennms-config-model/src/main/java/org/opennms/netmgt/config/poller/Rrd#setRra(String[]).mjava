    /**
     * 
     * 
     * @param rras
     */
    public void setRra(final String[] rras) {
        m_rras.clear();
        for (final String rra : rras) {
            m_rras.add(rra);
        }
    }

