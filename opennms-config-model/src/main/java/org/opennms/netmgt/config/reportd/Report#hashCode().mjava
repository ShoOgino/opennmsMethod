    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            reportTemplate, 
            reportName, 
            reportFormat, 
            reportEngine, 
            cronSchedule, 
            recipientList, 
            mailer, 
            parameterList);
        return hash;
    }

