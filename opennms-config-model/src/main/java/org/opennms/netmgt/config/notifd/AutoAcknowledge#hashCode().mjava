    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            resolutionPrefix, 
            uei, 
            acknowledge, 
            notify, 
            matchList);
        return hash;
    }

