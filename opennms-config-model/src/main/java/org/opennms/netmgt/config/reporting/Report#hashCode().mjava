    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            id, 
            type, 
            parameters, 
            pdfTemplate, 
            svgTemplate, 
            htmlTemplate, 
            logo);
        return hash;
    }

