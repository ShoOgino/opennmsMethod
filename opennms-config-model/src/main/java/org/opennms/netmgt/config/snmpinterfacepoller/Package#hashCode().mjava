    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    @Override
    public int hashCode() {
        int hash = Objects.hash(
            name, 
            filter, 
            specificList, 
            includeRangeList, 
            excludeRangeList, 
            includeUrlList, 
            _interfaceList);
        return hash;
    }

