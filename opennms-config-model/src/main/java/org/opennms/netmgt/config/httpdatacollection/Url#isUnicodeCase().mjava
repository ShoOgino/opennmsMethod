    /**
     * Gets the value of the unicodeCase property.
     * 
     * @return
     *     possible object is
     *     {@link Boolean }
     *     
     */
    public boolean isUnicodeCase() {
        if (unicodeCase == null) {
            return false;
        } else {
            return unicodeCase;
        }
    }

