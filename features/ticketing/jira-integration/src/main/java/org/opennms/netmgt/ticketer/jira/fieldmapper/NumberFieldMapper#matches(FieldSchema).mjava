    @Override
    public boolean matches(FieldSchema schema) {
        return "number".equals(schema.getType())
                || "com.attlassian.jira.plugin.system.customfieldtypes:float".equals(schema.getCustom());
    }

