    @Override
    public boolean matches(FieldSchema schema) {
        return ("assignee".equals(schema.getSystem()) && schema.getCustom() == null)
                || ("reporter".equals(schema.getSystem()) && schema.getCustom() == null)
                || "user".equals(schema.getType())
                || "user".equals(schema.getItems());
    }

