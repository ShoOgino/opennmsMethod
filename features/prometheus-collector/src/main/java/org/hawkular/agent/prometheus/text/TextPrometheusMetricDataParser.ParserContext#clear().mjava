        // starts a fresh metric family
        public void clear() {
            name = "";
            help = "";
            type = null;
            allowedNames.clear();
            textSamples.clear();
        }

