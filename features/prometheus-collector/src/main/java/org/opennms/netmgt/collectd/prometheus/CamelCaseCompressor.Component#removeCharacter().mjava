        public boolean removeCharacter() {
            if (value == null || value.length() < 1) {
                return false;
            }
            value = value.substring(0, value.length() -1);
            return true;
        }

