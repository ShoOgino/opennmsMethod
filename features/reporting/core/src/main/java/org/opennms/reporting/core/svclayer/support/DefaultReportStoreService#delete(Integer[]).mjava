    /**
     * <p>delete</p>
     *
     * @param ids an array of {@link java.lang.Integer} objects.
     */
    @Override
    public void delete(Integer[] ids) {
        for (Integer id : ids) {
            delete(id); 
        }
    }

