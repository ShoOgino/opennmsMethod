    /**
     * 
     * 
     * @param vRowArray
     */
    public void setRow(final Row[] vRowArray) {
        //-- copy array
        rowList.clear();
        
        for (int i = 0; i < vRowArray.length; i++) {
                this.rowList.add(vRowArray[i]);
        }
    }

