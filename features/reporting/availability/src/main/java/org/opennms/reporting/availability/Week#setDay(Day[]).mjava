    /**
     * 
     * 
     * @param vDayArray
     */
    public void setDay(final Day[] vDayArray) {
        //-- copy array
        dayList.clear();
        
        for (int i = 0; i < vDayArray.length; i++) {
                this.dayList.add(vDayArray[i]);
        }
    }

