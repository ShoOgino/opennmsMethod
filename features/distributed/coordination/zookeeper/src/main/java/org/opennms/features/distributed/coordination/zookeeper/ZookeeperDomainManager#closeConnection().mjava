    /**
     * Closes the connection.
     */
    private void closeConnection() {
        if (leaderSelector != null) {
            leaderSelector.close();
            leaderSelector = null;
        }

        client.close();
        client = null;
    }

