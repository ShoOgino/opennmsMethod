    public Collection<E> get(String key) {
        final Entries<E> entries = cache.getIfPresent(key);
        if (entries == null) {
            return null;
        }
        return entries.get();
    }

