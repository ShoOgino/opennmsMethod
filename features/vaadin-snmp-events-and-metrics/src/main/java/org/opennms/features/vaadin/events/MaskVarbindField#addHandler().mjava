    /**
     * Adds the handler.
     */
    private void addHandler() {
        Varbind v = new Varbind();
        v.setVbnumber(1); // A non null value is required here.
        container.addBean(v);
    }

