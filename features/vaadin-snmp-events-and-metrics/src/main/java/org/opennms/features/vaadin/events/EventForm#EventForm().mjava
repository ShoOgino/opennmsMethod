    /**
     * Instantiates a new event form.
     */
    public EventForm() {
        setCaption("Event Detail");
        setBuffered(true);
        setVisible(false);
        setFormFieldFactory(new EventFormFieldFactory());
        initToolbar();
    }

