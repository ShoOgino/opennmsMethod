        public void countDrainStats(boolean drain, long readings) {
            m_queueChecks++;
            if (drain) {
                m_queueDrains++;
                m_sentReadings += readings;
            }
        }

