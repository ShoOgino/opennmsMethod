    @Override
    public void sendEvent(Event event) {
        try {
            eventMgr.send(event);
        } catch (EventProxyException e) {
            LOG.error("Failed to send {} ", event, e);
        }
    }

