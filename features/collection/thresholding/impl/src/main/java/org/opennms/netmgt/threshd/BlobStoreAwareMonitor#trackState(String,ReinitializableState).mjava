    @Override
    public void trackState(String key, ReinitializableState state) {
        try {
            readLock.lock();
            stateMap.put(key, state);
        } finally {
            readLock.unlock();
        }
    }

