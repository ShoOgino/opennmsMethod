    private void drawGraph(Graph graph) {
		D3 circles = m_nodeGroup.selectAll(".little")
						.data(graph.getNodes())
						.enter()
						.append("circle")
						.attr("class", ".little")
						.attr("cx", getX())
						.attr("cy", getY())
						.attr("r", 9);
		
		
		
		
						
		
	}

