    @Override
    public String toString() {
        return MoreObjects.toStringHelper(this)
                .add("isStarred", isStarred)
                .add("slug", slug)
                .add("version", version)
                .toString();
    }

