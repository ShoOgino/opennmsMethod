    @After
    public void tearDown() {
        if (jestClient != null) {
            jestClient.shutdownClient();
        }
        if (eventToIndex != null) {
            eventToIndex.close();
        }
    }

