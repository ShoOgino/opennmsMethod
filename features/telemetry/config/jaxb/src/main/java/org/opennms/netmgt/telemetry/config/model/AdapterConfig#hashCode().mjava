    @Override
    public int hashCode() {
        return Objects.hash(
                this.name,
                this.className,
                this.enabled,
                this.parameters,
                this.packages);
    }

