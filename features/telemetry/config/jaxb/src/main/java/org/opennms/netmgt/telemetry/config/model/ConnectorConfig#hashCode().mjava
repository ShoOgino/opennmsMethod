    @Override
    public int hashCode() {
        return Objects.hash(
                this.name,
                this.className,
                this.serviceName,
                this.queue,
                this.enabled,
                this.parameters,
                this.packages);
    }

