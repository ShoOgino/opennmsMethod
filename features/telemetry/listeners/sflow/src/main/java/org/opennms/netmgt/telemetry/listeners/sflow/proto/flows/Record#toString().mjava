    @Override
    public String toString() {
        return MoreObjects.toStringHelper(this)
                .add("dataFormat", this.dataFormat)
                .add("data", this.data)
                .toString();
    }

