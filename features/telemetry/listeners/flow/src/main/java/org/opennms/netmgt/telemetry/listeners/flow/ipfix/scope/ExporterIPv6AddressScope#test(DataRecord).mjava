    @Override
    public boolean test(final DataRecord record) {
        // TODO fooker: Search record for exporterIPv6Address and prefer it
        return record.set.packet.sender.getAddress().equals(address);
    }

