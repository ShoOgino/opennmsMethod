    @Override
    public String toString() {
        return MoreObjects.toStringHelper(this)
                .add("flowSetId", flowSetId)
                .add("length", length)
                .toString();
    }

