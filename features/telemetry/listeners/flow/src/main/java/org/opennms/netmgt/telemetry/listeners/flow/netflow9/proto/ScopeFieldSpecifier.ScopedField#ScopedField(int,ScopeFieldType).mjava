        private ScopedField(final int length,
                            final ScopeFieldType type) {
            this.length = length;
            this.type = Objects.requireNonNull(type);
        }

