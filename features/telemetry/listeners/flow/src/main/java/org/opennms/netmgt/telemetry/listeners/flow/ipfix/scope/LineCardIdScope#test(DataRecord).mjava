    @Override
    public boolean test(final DataRecord record) {
        LOG.warn("Don't know how to handle scope: lineCardId - {}", this.lineCardId);

        // TODO fooker: Implement

        return true;
    }

