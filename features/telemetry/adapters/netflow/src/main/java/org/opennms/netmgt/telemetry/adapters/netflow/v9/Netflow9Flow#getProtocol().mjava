    @Override
    public Integer getProtocol() {
        return get(document, "elements", "PROTOCOL", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

