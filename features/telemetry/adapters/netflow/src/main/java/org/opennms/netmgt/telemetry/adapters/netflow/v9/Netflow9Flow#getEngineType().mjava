    @Override
    public Integer getEngineType() {
        return get(document, "elements", "ENGINE_TYPE", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

