    @Override
    public Long getLastSwitched() {
        return get(document, "elements", "LAST_SWITCHED", "v")
                .map(v -> v.asInt64().getValue())
                .orElse(null);
    }

