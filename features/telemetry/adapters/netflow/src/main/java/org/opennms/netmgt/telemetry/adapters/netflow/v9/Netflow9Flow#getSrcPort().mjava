    @Override
    public Integer getSrcPort() {
        return get(document, "elements", "L4_SRC_PORT", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

