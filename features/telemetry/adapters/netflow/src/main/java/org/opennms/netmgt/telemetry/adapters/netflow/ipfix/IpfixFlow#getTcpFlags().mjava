    @Override
    public Integer getTcpFlags() {
        return get(document, "elements", "tcpControlBits", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

