    @Override
    public Integer getOutputSnmp() {
        return get(document, "elements", "OUTPUT_SNMP", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

