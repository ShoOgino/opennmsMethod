    @Override
    public Integer getEngineId() {
        return get(document, "elements", "engineId", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

