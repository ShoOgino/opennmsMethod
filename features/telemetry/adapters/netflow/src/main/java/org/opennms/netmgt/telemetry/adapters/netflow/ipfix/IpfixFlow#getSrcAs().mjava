    @Override
    public Integer getSrcAs() {
        return get(document, "elements", "bgpSourceAsNumber", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

