    @Override
    public Long getFirstSwitched() {
        return get(document, "elements", "FIRST_SWITCHED", "v")
                .map(v -> v.asInt64().getValue())
                .orElse(null);
    }

