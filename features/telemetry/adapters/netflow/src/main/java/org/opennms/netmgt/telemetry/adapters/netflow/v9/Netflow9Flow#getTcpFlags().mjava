    @Override
    public Integer getTcpFlags() {
        return get(document, "elements", "TCP_FLAGS", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

