    @Override
    public Integer getEngineId() {
        return get(document, "elements", "ENGINE_ID", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

