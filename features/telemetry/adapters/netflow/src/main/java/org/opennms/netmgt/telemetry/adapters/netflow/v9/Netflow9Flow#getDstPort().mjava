    @Override
    public Integer getDstPort() {
        return get(document, "elements", "L4_DST_PORT", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

