    @Override
    public Integer getInputSnmp() {
        return get(document, "elements", "INPUT_SNMP", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

