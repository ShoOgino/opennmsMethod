    @Override
    public Integer getTos() {
        return get(document, "elements", "ipClassOfService", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

