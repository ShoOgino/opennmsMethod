    @Override
    public Integer getEngineType() {
        return get(document, "elements", "engineType", "v")
                .map(v -> (int) v.asInt64().getValue())
                .orElse(null);
    }

