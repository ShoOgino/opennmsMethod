    public static DatagramVersion from(final ByteBuf buffer) throws InvalidPacketException {
        final int value = (int) BufferUtils.uint32(buffer);
        switch (value) {
            case 5:
                return VERSION5;
            default:
                throw new InvalidPacketException(buffer, "Unknown value: {}", value);
        }
    }

