    public void stop() {
        if (server != null) {
            if(observer != null) {
                observer.onCompleted();
            }
            if(gnmiStream != null) {
                gnmiStream.onCompleted();
            }
            server.shutdown();
        }
    }

