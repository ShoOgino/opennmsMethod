    public void setCity(String city) {
        if(city != null && city.length() > 255) {
            this.city = city.substring(0, 254);
        } else {
            this.city = city;
        }
    }

