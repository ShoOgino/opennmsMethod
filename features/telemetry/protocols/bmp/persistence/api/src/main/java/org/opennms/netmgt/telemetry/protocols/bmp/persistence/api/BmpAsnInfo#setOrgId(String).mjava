    public void setOrgId(String orgId) {
        if(orgId != null && orgId.length() > 255) {
            this.orgId = orgId.substring(0, 254);
        } else {
            this.orgId = orgId;
        }
    }

