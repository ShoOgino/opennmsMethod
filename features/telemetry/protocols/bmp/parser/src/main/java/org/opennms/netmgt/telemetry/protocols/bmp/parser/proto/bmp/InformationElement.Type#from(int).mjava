        private static Type from(final int type) {
            switch (type) {
                case 0:
                    return STRING;
                case 1:
                    return SYS_DESCR;
                case 2:
                    return SYS_NAME;
                default:
                    throw new IllegalArgumentException("Unknown information type");
            }
        }

