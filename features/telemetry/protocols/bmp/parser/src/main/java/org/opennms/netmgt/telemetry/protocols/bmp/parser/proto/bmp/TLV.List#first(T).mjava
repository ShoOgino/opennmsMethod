        public Optional<V> first(final T type) {
            return  this.elements.stream()
                    .filter(e -> e.type == type)
                    .findFirst()
                    .map(e -> e.value);
        }

