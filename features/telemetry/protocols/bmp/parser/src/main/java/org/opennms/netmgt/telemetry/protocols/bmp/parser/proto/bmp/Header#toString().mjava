    @Override
    public String toString() {
        return MoreObjects.toStringHelper(this)
                .add("version", version)
                .add("length", length)
                .add("type", type)
                .toString();
    }

