            private static Type from(final int type) {
                switch (type) {
                    case 0:
                        return BGP_MESSAGE;
                    case 1:
                        return INFORMATION;
                    default:
                        throw new IllegalArgumentException("Unknown message type");
                }
            }

