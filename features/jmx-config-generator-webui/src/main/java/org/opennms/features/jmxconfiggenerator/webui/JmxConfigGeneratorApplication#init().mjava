	@Override
	public void init() {
		setTheme(Config.STYLE_NAME);
		initHeaderPanel();
		initContentPanel();
		initMainWindow();

		registerListener(UiModel.class, this);
		updateView(UiState.IntroductionView);
	}

