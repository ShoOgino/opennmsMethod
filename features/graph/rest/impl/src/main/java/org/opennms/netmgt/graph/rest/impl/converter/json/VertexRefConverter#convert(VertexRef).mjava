    @Override
    public JSONObject convert(VertexRef input) {
        final JSONObject jsonObject = new JSONObject();
        jsonObject.put("namespace", input.getNamespace());
        jsonObject.put("id", input.getId());
        return jsonObject;
    }

