	/**
	 * <p>setVisible</p>
	 *
	 * @param visible a boolean.
	 */
	public void setVisible(final boolean visible) {
	    if(m_visible != visible) {
	        m_dirty = true;
	    }
		m_visible  = visible;
	}

