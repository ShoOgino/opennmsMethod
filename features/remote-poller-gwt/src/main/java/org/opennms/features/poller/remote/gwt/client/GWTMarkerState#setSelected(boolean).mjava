	/**
	 * <p>setSelected</p>
	 *
	 * @param selected a boolean.
	 */
	public void setSelected(final boolean selected) {
	    if(m_selected != selected ) {
	        m_dirty = true;
	    }
		m_selected = selected;
	}

