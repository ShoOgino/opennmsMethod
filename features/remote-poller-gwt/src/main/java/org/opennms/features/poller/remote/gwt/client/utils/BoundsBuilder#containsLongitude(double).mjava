    private boolean containsLongitude(double lng) {
        if (swLng <= neLng) {
            return swLng <= lng && lng <= neLng;
        } else {
            return !(neLng < lng && lng < swLng);
        }
    }

