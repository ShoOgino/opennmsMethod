    @Override
    protected void execute() throws CmdLineException, CmdRunException {
        if (showDictionary) {
            printDictionary();
            return;
        }
        super.execute();
    }

