    @Test
    public void verifyContextPath() {
        for (ApiVersion eachVersion : ApiVersion.values()) {
            Assert.assertTrue("The context path must start with a '/'", eachVersion.getContextPath().startsWith("/"));
            Assert.assertTrue("The context path must end with a '/'", eachVersion.getContextPath().endsWith("/"));
        }
    }

