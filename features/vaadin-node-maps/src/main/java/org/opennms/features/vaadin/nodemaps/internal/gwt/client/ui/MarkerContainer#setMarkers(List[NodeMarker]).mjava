    public void setMarkers(final List<NodeMarker> markers) {
        if (m_markers != markers) {
            m_markers.clear();
            m_markers.addAll(markers);
        }
        refresh();
    }

