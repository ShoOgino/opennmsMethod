    @Override
    public JmxConfigGeneratorUI getUI() {
        if (!ui.isAttached()) {
            throw new IllegalStateException("UI " + ui.getUIId() + " is detached.");
        }
        return ui;
    }

