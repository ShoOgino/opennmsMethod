    @Override
    public JmxConfigGeneratorUI getUI() {
        if (ui.isAttached()) {
            return ui;
        }
        throw new IllegalStateException("UI " + ui.getUIId() + " is not attached");
    }

