    /**
     * Checks a metric.
     *
     * @param metric the metric
     */
    public void checkMetric(String metric) {
        metricsMap.putIfAbsent(metric, true);
        metricsMeter.mark();
    }

