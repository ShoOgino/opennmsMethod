    public RowDef getRowDef(String label) {
        for (RowDef rowDef : getRows()) {
            if (label.equals(rowDef.getLabel())) {
                return rowDef;
            }
        }
        return null;
    }

