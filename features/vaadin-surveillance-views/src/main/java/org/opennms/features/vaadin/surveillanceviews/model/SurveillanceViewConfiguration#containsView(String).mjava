    public boolean containsView(String name) {
        for (View view : getViews()) {
            if (name.equals(view.getName())) {
                return true;
            }
        }
        return false;
    }

