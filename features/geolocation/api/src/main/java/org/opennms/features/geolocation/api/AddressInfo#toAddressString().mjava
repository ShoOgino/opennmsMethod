    public String toAddressString() {
        return join(", ",
            join(" ", getAddress1(), getAddress2()),
            getCity(),
            join(" ", getState(), getZip()),
            getCountry());
    }

