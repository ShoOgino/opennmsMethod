    public void setSeverity(OnmsSeverity severity) {
        if (severity == null) {
            severities.clear();
        } else {
            setSeverities(Lists.newArrayList(severity));
        }
    }

