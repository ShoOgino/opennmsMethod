    public void setTos(final Integer tos) {
        if (tos != null) {
            setDscp((tos & 0b11111100) >> 2);
            setEcn(tos & 0b00000011);
        } else {
            setDscp(null);
            setEcn(null);
        }
        this.tos = tos;
    }

