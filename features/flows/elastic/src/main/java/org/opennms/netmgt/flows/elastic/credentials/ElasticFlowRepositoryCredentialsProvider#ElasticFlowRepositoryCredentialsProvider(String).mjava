    public ElasticFlowRepositoryCredentialsProvider(String configName) {
        Path configPath = Paths.get(System.getProperty("opennms.home"), "etc", configName);
        this.configPath = configPath;
    }

