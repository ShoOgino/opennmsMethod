    private Rule createAndAddRule(String name, int position) {
        Rule rule = new Rule();
        rule.setName(name);
        rule.setPosition(position);
        rule.setId(idCounter++);
        rules.add(rule);
        group.addRule(rule);
        return rule;
    }

