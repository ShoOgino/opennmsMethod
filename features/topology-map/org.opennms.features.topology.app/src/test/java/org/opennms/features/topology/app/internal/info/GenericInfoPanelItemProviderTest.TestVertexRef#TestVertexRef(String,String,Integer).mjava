        public TestVertexRef(String id, String label, Integer nodeId) {
            super("nodes", id, label);

            if (nodeId != null) {
                setNodeID(nodeId);
            }
        }

