        public GVertex(String key, Object itemId, Item item, String groupKey, Object groupId) {
            super("simple", key);
            m_itemId = itemId;
            m_item = item;
            m_groupKey = groupKey;
            m_groupId = groupId;
        }

