        protected void offset(double x, double y)
        {
            String before = this.toString();
            this.x += x;
            this.y += y;
            String after = this.toString();
            print(before, after);
        }

