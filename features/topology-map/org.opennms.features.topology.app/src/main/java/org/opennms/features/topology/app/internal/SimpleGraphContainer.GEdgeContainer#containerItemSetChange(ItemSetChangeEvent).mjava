        @Override
        public void containerItemSetChange(ItemSetChangeEvent event) {
            m_edgeHolder.update();
            removeAllItems();
            addAll(m_edgeHolder.getElements());
            fireItemSetChange();
        }

