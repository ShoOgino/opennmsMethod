        @Override
        public void setLocation(Point2D p) {
            String before = this.toString();
            super.setLocation(p);
            String after = this.toString();
            print(before, after);
        }

