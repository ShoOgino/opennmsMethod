    @Override
    public void setInterval(long secondsToWait) {
        // We do not allow to set < 5 Seconds
        if (secondsToWait < 5) {
            secondsToWait = 5;
        }
        interval = secondsToWait;
    }

