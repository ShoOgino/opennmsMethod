    @Override
    protected boolean isChecked(GraphContainer container) {
        if(container.getLayoutAlgorithm() instanceof ManualLayoutAlgorithm) {
            return true;
        }
        return false;
    }

