	private void setGraph(GWTGraph graph) {
		m_graph = graph;
		
		makeGlobal("panTo", getPanToSelection());
		if(getPanToSelection()) {
		    repaintGraphNow();
		    centerSelection();
		}else {
		    repaintGraph();
		}
		
	}

