	private void singleSelectEdge(String edgeId) {
	    deselectAllVertices();
	    deselectAllEdges();
	    
	    if(edgeId.isEmpty()) {
	        requestRepaint();
	    }else {
	        toggleSelectedEdge(edgeId);
	    }
	}

