    @Override
    public void onGraphUpdated(GWTGraph graph) {
            m_presenter.getViewRenderer().draw(graph, this);
            
            if(m_isRefresh) {
                fitToScreen();
                m_isRefresh = false;
            }
            
    }

