        public GEdge(String key, Object itemId, Item item, GVertex source, GVertex target) {
            super("simple", key);
            m_itemId = itemId;
            m_item = item;
            m_source = source;
            m_target = target;
        }

