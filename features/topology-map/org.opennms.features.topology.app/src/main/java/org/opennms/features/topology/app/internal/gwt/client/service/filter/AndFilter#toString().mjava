    @Override
    public String toString() {
        StringBuilder buf = new StringBuilder();
        buf.append("(&");
        for(Filter f : m_filters) {
            buf.append(f);
        }
        buf.append(")");
        return buf.toString();
    }

