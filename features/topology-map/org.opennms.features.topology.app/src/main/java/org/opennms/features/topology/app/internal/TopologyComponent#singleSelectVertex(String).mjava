    private void singleSelectVertex(String vertexId) {
        deselectAllEdges();
        deselectAllVertices();
        
        if(vertexId.isEmpty()) {
            requestRepaint();
        } else {
            toggleSelectedVertex(vertexId);
        }
    }

