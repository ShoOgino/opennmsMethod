    private void singleSelectVertex(String vertexKey) {
        deselectAllEdges();
        deselectAllVertices();
        
        if(vertexKey.isEmpty()) {
            requestRepaint();
        } else {
            toggleSelectedVertex(vertexKey);
        }
    }

