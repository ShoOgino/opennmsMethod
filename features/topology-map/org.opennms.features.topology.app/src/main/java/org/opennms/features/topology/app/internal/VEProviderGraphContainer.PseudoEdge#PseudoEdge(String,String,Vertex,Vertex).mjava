        public PseudoEdge(String namespace, String id, Vertex source, Vertex target) {
        	m_namespace = namespace;
        	m_id = id;
            m_source = source;
            m_target = target;
        }

