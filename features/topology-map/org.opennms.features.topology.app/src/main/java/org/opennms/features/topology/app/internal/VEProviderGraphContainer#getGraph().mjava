    @Override
    public Graph getGraph() {
        synchronized(m_containerDirty) {
            if (isDirty() || isCriteriaDirty()) {
                rebuildGraph();
                setDirty(false);
                setCriteriaDirty(false);
            }
        }
        return m_graph;
    }

