    public String getQueryPrefix(String query){
        String prefix = null;
        if(query.contains("=")){
            prefix = query.substring(0, query.indexOf('=') + 1);
        }
        return prefix;
    }

