	@Override
	public final int getSemanticZoomLevel() {
		return m_semanticZoomLevel >= 0
		? m_semanticZoomLevel
				: getParent() == null 
				? 0 
						: getParent().getSemanticZoomLevel() + 1;
	}

