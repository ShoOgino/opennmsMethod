        @Override
        public int hashCode() {
            final int prime = 31;
            int result = 1;
            result = prime * result + ((m_vertices == null) ? 0 : m_vertices.hashCode());
            return result;
        }

