    public int getSemanticZoomLevel() {
        Property szlProperty = getItem().getItemProperty(SEMANTIC_ZOOM_LEVEL);
        return szlProperty == null ? 0 : (Integer)szlProperty.getValue();
    }

