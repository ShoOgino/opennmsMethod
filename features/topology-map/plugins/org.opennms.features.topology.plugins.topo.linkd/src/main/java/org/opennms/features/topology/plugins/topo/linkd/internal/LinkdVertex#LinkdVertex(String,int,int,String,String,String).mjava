	public LinkdVertex(String id, int x, int y, String iconKey, String label, String ipAddr) {
		super("linkd", id);
		setIconKey(iconKey);
		setLabel(label);
		setIpAddress(ipAddr);
		setX(x);
		setY(y);
	}

