    @Override
    public boolean equals(Object obj) {
        if(obj instanceof LldpLinkdDetail){
            LldpLinkdDetail objDetail = (LldpLinkdDetail)obj;

            return getId().equals(objDetail.getId());
        } else  {
            return false;
        }

    }

