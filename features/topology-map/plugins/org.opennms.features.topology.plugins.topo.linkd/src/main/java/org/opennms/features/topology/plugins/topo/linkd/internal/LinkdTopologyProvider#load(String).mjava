    @Override
    public void load(String filename) throws MalformedURLException, JAXBException {
        if (filename == null) {
            loadtopology();
        } else {
            loadfromfile(filename);
        }
    }

