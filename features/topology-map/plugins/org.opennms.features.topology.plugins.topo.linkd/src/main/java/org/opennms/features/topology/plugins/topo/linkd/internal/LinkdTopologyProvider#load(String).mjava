    @Override
    public void load(String filename) {
        reset();
        if (filename == null) {
            loadtopology();
        } else {
            loadfromfile(filename);
        }
    }

