	public void add(String s){
		Key newKey = new Key(s);
		if (head == null){
			head = newKey;
			tail = newKey;
		} else {
			tail.setPrev(newKey);
			newKey.setNext(tail);
			tail = newKey;
		}
		size++;
	}

