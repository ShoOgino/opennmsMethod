	public String pop(){
		if (head != null){
			String headValue = head.getValue();
			if (head.getPrev() != null){
				head = head.getPrev();
			} else {
				head = null;
				tail = null;
			}
			size--;
			return headValue;
		} else return null;
	}

