    public synchronized boolean setSize(int w, int h) {
        if (w < 2 || w > 256 || h < 2 || h > 256) {
            return false;
        }
        this.width = w;
        this.height = h;
        reset_screen();
        return true;
    }

