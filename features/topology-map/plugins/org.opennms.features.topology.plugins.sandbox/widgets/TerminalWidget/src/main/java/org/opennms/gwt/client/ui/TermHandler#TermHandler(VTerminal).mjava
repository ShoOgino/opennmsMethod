	public TermHandler(VTerminal vTerm){
		this.vTerm = vTerm;
		keybuf = new KeyBuffer();
		code = null;
		isClosed = false;
		updateTimer = new Timer() {
			@Override
			public void run() {
				update();
			}
		};
	}

