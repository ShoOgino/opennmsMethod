	public SSHTerminal(int width, int height) {
		super();
		TERM_WIDTH = width;
		TERM_HEIGHT = height;
		dumpContents = null;
		try {
			st = new SessionTerminal();
			st.start();
			forceUpdate = true;
		} catch (IOException e) { e.printStackTrace(); }
	}

