  protected void cursor_address(int r, int c){
    term.draw_cursor();
    x=(c-1)*char_width;
    y=r*char_height;
    //System.out.println("setCourosr: "+x+" "+y);
    term.setCursor(x, y);
    term.draw_cursor();
  }

