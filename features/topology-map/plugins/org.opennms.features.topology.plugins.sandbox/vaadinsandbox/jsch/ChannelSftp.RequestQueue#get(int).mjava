    Request get(int id){
      count -= 1;
      int i=head;
      head++; 
      if(head==rrq.length) head=0;
      if(rrq[i].id!=id){
        System.err.println("The request is not in order.");
      }
      rrq[i].id=0;
      return rrq[i];
    }

