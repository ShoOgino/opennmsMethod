  private void repaint(final int x, final int y, final int width,
      final int height){
    Display display=Display.getDefault();
    if(display==null)
      return;

    rh.x=x;
    rh.y=y;
    rh.width=width;
    rh.height=height;
    display.syncExec(rh);
  }

