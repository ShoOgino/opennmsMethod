    private OnmsNode getParent() {
        if (node.getParent() != null) {
            return node.getParent();
        }
        node.setParent(new OnmsNode());
        return node.getParent();
    }

