    @Override
    public void resetContainer() {
        m_vertexContainer.removeAllItems();
        m_edgeContainer.removeAllItems();
        
        m_counter = 0;
        m_edgeCounter = 0;
    }

