        public SimpleGraph(String namespace, List<Vertex> vertices, List<SimpleEdge> edges) {
        	m_namespace = namespace;
            m_vertices = vertices;
            m_edges = edges;
        }

