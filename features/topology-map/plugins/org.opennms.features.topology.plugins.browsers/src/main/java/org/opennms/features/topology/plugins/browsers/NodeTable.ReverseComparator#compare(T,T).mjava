		@Override
		public int compare(T o1, T o2) {
			int comparison = m_comparator.compare(o1, o2);
			if (comparison == 0) {
				return 0;
			} else if (comparison < 0) {
				return 1;
			} else {
				return -1;
			}
		}

