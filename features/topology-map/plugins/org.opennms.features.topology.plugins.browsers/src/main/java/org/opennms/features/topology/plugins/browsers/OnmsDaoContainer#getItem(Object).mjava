	@Override
	public Item getItem(Object itemId) {
		if (itemId == null) {
			return null;
		}
		T bean = m_dao.get((K)itemId);
		if (bean == null) {
			return null;
		} else {
			return new BeanItem<T>(bean);
		}
	}

