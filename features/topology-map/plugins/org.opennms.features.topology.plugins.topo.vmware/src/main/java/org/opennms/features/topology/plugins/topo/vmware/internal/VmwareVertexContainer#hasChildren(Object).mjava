    public boolean hasChildren(Object itemId) {
        if (!containsId(itemId)) {
            return false;
        }
        VmwareVertex v = getItem(itemId).getBean();
        return !v.isLeaf();
    }

