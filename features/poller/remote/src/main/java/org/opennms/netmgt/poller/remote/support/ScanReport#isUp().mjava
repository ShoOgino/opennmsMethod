	public boolean isUp() {
	    if (m_pollResults != null) {
	        for (final PollResult result : m_pollResults) {
	            if (!result.isUp()) {
	                return false;
	            }
	        }
	    }
	    return true;
	}

