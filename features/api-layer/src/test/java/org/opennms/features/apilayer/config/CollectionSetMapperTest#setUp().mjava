    @Before
    public void setUp() {
        node = ImmutableNode.newBuilder()
                .setId(36)
                .setForeignSource("fs")
                .setForeignId("fid")
                .setLabel(NODE_LABEL)
                .build();
    }

