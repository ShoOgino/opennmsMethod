    /**
     * Sets up the tests.
     */
    @Before
    public void setUp() {
        MockLogAppender.setupLogging();
        parser = new JsmiMibParser();
        parser.setMibDirectory(MIB_DIR);
    }

