    @Test
    public void shouldAggregateToMin() {
        sample(10, 10);
        sample(12, 15);
        sample(13, 20);
        sample(16, 13);
        compute(Aggregation.MIN, 10, 19,5);
        assertEquals(2, results.size());
        expect(0, 10, 10);
        expect(1, 15, 13);
    }

