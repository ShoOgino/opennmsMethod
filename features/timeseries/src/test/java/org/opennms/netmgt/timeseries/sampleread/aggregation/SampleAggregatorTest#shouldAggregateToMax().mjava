    @Test
    public void shouldAggregateToMax() {
        sample(10, 10);
        sample(12, 15);
        sample(13, 20);
        sample(16, 13);
        compute(Aggregation.MAX, 10, 19, 5);
        assertEquals(2, results.size());
        expect(0, 10, 20);
        expect(1, 15, 13);
    }

