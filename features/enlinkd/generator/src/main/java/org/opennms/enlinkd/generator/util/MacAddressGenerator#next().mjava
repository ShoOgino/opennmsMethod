    public String next() {
        String s = String.format("%1$" + 12 + "s", Integer.toHexString(next)); // example: "           1"
        s = s.replace(' ', '0'); // example: "000000000001"
        next++;
        return s;
    }

