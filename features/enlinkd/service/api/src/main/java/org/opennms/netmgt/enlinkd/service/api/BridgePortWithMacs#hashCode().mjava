    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result + ((m_port == null) ? 0 : m_port.hashCode());
        return result;
    }

