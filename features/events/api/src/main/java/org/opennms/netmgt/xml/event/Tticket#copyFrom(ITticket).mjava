    public static Tticket copyFrom(ITticket source) {
        if (source == null) {
            return null;
        }

        Tticket tticket = new Tticket();
        tticket.setContent(source.getContent());
        tticket.setState(source.getState());
        return tticket;
    }

