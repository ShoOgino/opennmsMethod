        public Builder(IValue value) {
            content = value.getContent();
            type = value.getType();
            encoding = value.getEncoding();
            expand = value.isExpand();
        }

