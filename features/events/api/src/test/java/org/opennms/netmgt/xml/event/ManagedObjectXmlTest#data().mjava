    @Parameters
    public static Collection<Object[]> data() throws ParseException {
        ManagedObject managedObject = new ManagedObject();
        managedObject.setType("node");
        return Arrays.asList(new Object[][]{
                {
                        managedObject,
                        "<managed-object type=\"node\"/>",
                        null,}
        });
    }

