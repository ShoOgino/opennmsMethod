    /**
     * <p>onStop</p>
     */
    @Override
    protected void onStop() {
        if (m_receiver != null) {
            m_receiver.close();
        }
        LOG.debug("Eventd stopped");
    }

