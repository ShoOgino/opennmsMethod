    /**
     * Returns true if this runnable is executing.
     *
     * @return a boolean.
     */
    public boolean isAlive() {
        boolean rc = false;
        if (m_context != null) {
            rc = m_context.isAlive();
        }

        return rc;
    }

