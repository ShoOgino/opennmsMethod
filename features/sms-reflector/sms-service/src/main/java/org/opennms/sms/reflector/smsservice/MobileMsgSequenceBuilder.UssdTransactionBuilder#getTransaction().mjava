		@Override
		public MobileMsgTransaction getTransaction() {
			return new UssdTransaction(
				m_sequence,
				getLabel(),
				getGatewayId(),
				getTimeout(),
				getRetries(),
				m_text,
				getMatcher()
			);
		}

