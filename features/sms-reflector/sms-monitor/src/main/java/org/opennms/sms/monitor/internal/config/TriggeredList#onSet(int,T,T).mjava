    protected void onSet(int index, T oldElem, T newElem) {
        if (oldElem != null) {
            onRemove(index, oldElem);
        }
        if (newElem != null) {
            onAdd(index, newElem);
        }
    }

