    @Transient
    private Set<Long> getChildServiceIds() {
        return m_childServices.stream()
                              .map(svc -> svc.getId())
                              .collect(Collectors.toSet());
    }

