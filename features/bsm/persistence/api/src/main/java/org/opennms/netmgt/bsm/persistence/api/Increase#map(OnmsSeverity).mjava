    @Override
    public Optional<OnmsSeverity> map(OnmsSeverity source) {
        if (source == null) {
            return Optional.empty();
        }
        int newId = Math.min(OnmsSeverity.CRITICAL.getId(), source.getId() + 1);
        return Optional.of(OnmsSeverity.get(newId));
    }

