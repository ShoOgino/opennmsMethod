    @Override
    public Optional<OnmsSeverity> map(OnmsSeverity source) {
        if (source == null) {
            return Optional.empty();
        } else {
            return Optional.of(source);
        }
    }

