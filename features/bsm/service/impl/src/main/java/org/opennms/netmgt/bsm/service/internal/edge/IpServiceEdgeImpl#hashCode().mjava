    @Override
    public int hashCode() {
        if (getId() != null) {
            return getId().hashCode();
        }
        return super.hashCode(); //Objects.hash(getIpService(), super.hashCode());
    }

