    @Override
    public Set<String> getReductionKeys() {
        Set<String> keys = Sets.newHashSet();
        m_entity.getEdges(SingleReductionKeyEdge.class).forEach(e -> keys.add(e.getReductionKey()));
        return keys;

    }

