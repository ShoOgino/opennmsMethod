    @Override
    public Optional<Status> map(Status source) {
        if (source == null) {
            return Optional.empty();
        } else {
            return Optional.of(source);
        }
    }

