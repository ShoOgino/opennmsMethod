    /**
     * <p>
     * Close snmp session
     * </p>
     * 
     * @param ifindex
     *            interface index to get
     * 
     * @return The status of interface in human format
     */
    public void close() {
        try {
            m_session.close();
        } catch (Exception e) {
            System.out.println(e.getMessage());
        }
    }

