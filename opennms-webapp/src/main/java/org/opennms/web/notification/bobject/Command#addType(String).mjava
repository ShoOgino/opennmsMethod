    /**
     * Adds an identifier type for command
     *
     * @param aType
     *            the notification type
     */
    public void addType(String aType) {
        m_types.add(aType);
    }

