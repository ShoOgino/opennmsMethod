    private static String getKey(final String s) {
        final int i = s.indexOf('=');
        if (i >= 0) {
            return s.substring(0, i);
        } else {
            return s;
        }
    }

