    private static String getValue(final String s) {
        final int i = s.indexOf('=');
        if (i >= 0) {
            return s.substring(i + 1);
        } else {
            return "";
        }
    }

