    @Override
    public String getSQLTemplate() {
        if(getValue() == null) {
            return " " + getSQLFieldName() + " IS NULL ";
        }
        return " " + getSQLFieldName() + " =  %s ";
    }

