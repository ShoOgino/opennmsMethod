    @Override
    public String getSQLTemplate() {
        if(getValue() == null) {
            return " " + getSQLFieldName() + " IS NOT NULL ";
        }
        return " " + getSQLFieldName() + " <> %s OR IS NULL";
    }

