    private List<T> getValuesAsList(){
        List<T> ids = new ArrayList<T>();
        
        for(int i = 0; i < m_values.length; i++){
            ids.add(m_values[i]);
        }
        
        return ids;
    }

