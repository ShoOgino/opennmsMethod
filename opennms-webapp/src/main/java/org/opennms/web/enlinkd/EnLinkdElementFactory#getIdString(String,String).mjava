    private String getIdString(String addrtype, String addr) {
        StringBuffer sb = new StringBuffer();
        if (addrtype != null ) {
            sb.append(addrtype);
            sb.append(":");
            sb.append(addr);
        }
        return sb.toString();
    }

