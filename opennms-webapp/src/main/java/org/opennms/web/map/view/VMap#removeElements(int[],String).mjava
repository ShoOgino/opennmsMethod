    public void removeElements(int[] ids, String type) {
        if(ids!=null){
	        for (int i = 0; i < ids.length; i++) {
	            removeElement(ids[i],type);
	        }
        }
    }

