    public VLink[] getAllLinks() {
    	if(links.size()==0){
    		return null;
    	}
    	VLink[] arrayLinks = new VLink[links.size()];
    	links.values().toArray(arrayLinks);
        return arrayLinks;
    } 

