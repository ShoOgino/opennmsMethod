        AlarmView() {
            initializeTable();
            
            m_pager = new Pager(this);
            
            m_panel.add(m_alarmTable);
            m_panel.add(m_pager);
            initWidget(m_panel);
        }

