        protected void cellClicked(int row, int col) {
            
            clearSelection();
            
            m_selectedRow = row;
            m_selectedCol = col;

            setSelection(row, col);
        }

