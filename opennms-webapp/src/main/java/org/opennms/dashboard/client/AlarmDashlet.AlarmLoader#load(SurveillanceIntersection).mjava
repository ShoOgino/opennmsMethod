        public void load(final SurveillanceIntersection intersection) {
            setStatus("Loading...");
            Timer timer = new Timer() {

                public void run() {
                    loaded(intersection);
                }
                
            };
            timer.schedule(3000);
            
        }

