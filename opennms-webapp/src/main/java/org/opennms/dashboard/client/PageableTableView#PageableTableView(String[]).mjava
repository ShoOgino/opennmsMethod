	PageableTableView(String[] headings) {
        initializeTable(headings);
        
        m_pager = new Pager(this);
        
        m_panel.add(m_table);
        m_panel.add(m_pager);
        initWidget(m_panel);
    }

