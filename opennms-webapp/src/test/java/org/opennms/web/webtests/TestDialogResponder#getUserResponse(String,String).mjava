    public String getUserResponse(String prompt, String defaultResponse) {
        String response = (String)m_userResponses.get(prompt);
        if (response == null)
            Assert.fail("Unanticipated User Query '"+prompt+"'");
        return response;
    }

