    public void tearDown() {
        System.err.println("------------------------------------------------------------------------------");
        contextListener.contextDestroyed(new ServletContextEvent(servletContext));
        dispatcher.destroy();
    }

