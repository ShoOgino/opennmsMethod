        public void simulate(final KieSession kieSession) {
            for (final SimItem item : m_eventSequence) {
                item.simulate(kieSession);
                System.out.println("Memory Size = " + kieSession.getFactCount());
            }
        }

