    /**
     *
     *
     * @param vGlobalArray
     */
    public void setGlobal(final Global[] vGlobalArray) {
        _globalList.clear();
        this._globalList.addAll(Arrays.asList(vGlobalArray));
    }

