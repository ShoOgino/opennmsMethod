            public void simulate(final KieSession kieSession) {
                sleep(m_delay);
                System.out.println("Start simulation of " + this);
                kieSession.insert(m_event);
                kieSession.fireAllRules();
                System.out.println("End simulation of " + this);
            }

