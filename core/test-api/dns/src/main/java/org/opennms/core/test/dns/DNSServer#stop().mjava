    public void stop() {
        for (final Stoppable listener : m_activeListeners) {
            LOG.debug("stopping {}", listener);
            listener.stop();
            LOG.debug("stopped {}", listener);
        }
    }

