    public static MockLogAppender getInstance() {
        setupLogging();

        if (s_instance == null) {
            s_instance = new MockLogAppender();
        }

        return s_instance;
    }

