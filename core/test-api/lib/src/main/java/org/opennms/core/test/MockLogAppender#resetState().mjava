    /**
     * <p>resetEvents</p>
     */
    public static void resetState() {
        s_highestLoggedLevel.set(Level.TRACE);
        synchronized(s_events) {
            s_events.clear();
        }
    }

