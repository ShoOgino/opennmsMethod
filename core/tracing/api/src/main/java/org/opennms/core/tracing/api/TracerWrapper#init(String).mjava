    /**
     * Initialize tracer configuration and return a tracer for the service provided.
     * @param serviceName service name, typically one service per JVM.
     * @return Tracer  return configured tracer.
     */
    Tracer init(String serviceName);

