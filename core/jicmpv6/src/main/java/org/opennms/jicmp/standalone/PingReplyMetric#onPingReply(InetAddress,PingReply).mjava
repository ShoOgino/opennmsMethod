    @Override
    public void onPingReply(InetAddress address, PingReply reply) {
        try {
            update(reply.getElapsedTimeNanos());
        } finally {
            m_latch.countDown();
        }
    }

