        /**
         * <p>finalize</p>
         *
         * @throws java.sql.SQLException if any.
         */
        protected void finalize() throws SQLException {
            if (m_statement != null) {
                m_statement.close();
            }
            if (m_connection != null) {
                m_connection.close();
            }
        }

