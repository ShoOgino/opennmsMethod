    /**
     * Method hashCode.
     * 
     * @return a hash code value for the object.
     */
    public int hashCode() {
        int hash = Objects.hash(
            name, 
            databaseName, 
            schemaName, 
            url, 
            className, 
            userName, 
            password, 
            paramList);
        return hash;
    }

