    /**
     * <p>getConfig</p>
     *
     * @return Returns the config.
     */
    protected V getObject() {
        getReadLock().lock();
        try {
            return getContainer().getObject();
        } finally {
            getReadLock().unlock();
        }
    }

