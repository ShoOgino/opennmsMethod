    @Override
    public boolean equals(Object o) {
        if (o instanceof Filter) {
            Filter f = (Filter)o;
            return toString().equals(f.toString());
        }
        return false;
    }

