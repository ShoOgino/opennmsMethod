    @Override
    public void add(final K key, final V value) {
        if (!containsKey(key)) {
            final LinkedHashSet<V> valueList = new LinkedHashSet<>();
            valueList.add(value);
            put(key, valueList);
        } else {
            get(key).add(value);
        }
    }

