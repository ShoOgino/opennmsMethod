    private String escaped(String value) {
        return value
            .replace("\\", "\\\\")
            .replace("*", "\\*")
            .replace(")", "\\)")
            .replace("(", "\\(")
            ;
    }

