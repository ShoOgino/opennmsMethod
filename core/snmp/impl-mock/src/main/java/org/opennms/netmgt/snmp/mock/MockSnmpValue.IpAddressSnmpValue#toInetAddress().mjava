            @Override
        public InetAddress toInetAddress() {
            try {
                return addr(toString());
            } catch (Exception e) {
                return super.toInetAddress();
            }
        }

