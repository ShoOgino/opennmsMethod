    public SNMPWalkWithTrackerBuilder(DelegatingLocationAwareSnmpClientImpl client, SnmpAgentConfig agent, CollectionTracker tracker) {
        super(client, agent, Collections.emptyList(), buildWalkRequests(tracker));
        m_tracker = tracker;
    }

