    private void finish() {
        signal();
        try {
            close();
        } catch (IOException e) {
            LOG.error(getName()+": Unexpected Error occured closing SNMP session for: "+m_address, e);
        }
    }

