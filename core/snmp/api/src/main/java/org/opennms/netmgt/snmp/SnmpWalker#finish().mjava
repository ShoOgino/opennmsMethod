    private void finish() {
        signal();
        try {
            close();
        } catch (IOException e) {
            LOG.error("{}: Unexpected Error occured closing SNMP session for: {}", getName(), m_address, e);
        }
    }

