    private void finish() {
        signal();
        try {
            close();
        } catch (IOException e) {
            s_log.error(getName()+": Unexpected Error occured closing SNMP session for: "+m_address, e);
        }
    }

