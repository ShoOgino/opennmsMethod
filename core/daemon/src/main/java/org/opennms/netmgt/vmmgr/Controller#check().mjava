    /**
     * <p>check</p>
     *
     * @return a int.
     */
    public int check() {
        try {
            new DatabaseChecker().check();
        } catch (final Throwable t) {
            LogUtils.errorf(this, t, "error invoking check command");
            return 1;
        }
        return 0;
    }

