    /**
     * <p>check</p>
     *
     * @return a int.
     */
    public int check() {
        try {
            new DatabaseChecker().check();
        } catch (final Throwable t) {
        	LOG.error("error invoking check command", t);
            return 1;
        }
        return 0;
    }

