    @Override
    public CamelContext getContext() {
        SimpleRegistry registry = new SimpleRegistry();
        registry.put("connectionFactory", sqsManager.getSQSConnectionFactory());
        return new DefaultCamelContext(registry);
    }

