    @Override
    public Tracer getTracer(String serviceName) {
        if (tracerWrapper != null) {
            return  tracerWrapper.init(serviceName);
        }
        return GlobalTracer.get();
    }

