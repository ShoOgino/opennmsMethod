    public void destroy() {
        if (producer != null) {
            producer.close();
        }
         messageCache.clear();
         executor.shutdown();
         delayQueueExecutor.shutdown();
    }

