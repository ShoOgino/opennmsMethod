    public int getNumOfMessages(String moduleName) {

        BlockingQueue<String> queue = queueMap.get(moduleName);
        if (queue != null) {
            return queue.size();
        }
        return 0;
    }

