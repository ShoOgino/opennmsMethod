        private void markFull() {
            if (!isFull) {
                RATE_LIMITED_LOGGER.info("Off heap file for module {} is now full", moduleName);
                isFull = true;
            }
        }

