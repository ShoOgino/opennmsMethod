    @After
    public void tearDown() throws Exception {
        for (HeartbeatGenerator generator : generators) {
            generator.stop();
        }
        generators.clear();
        super.tearDown();
    }

