    public void shutdown() {
        executor.shutdown();
        if (jmxReporter != null) {
            jmxReporter.close();
        }
        if(getStartupExecutor() != null) {
            getStartupExecutor().shutdown();
        }
    }

