    public void destroy() {
        if (reporter != null) {
            reporter.close();
            reporter = null;
        }

        if (producer != null) {
            producer.close();
            producer = null;
        }
    }

