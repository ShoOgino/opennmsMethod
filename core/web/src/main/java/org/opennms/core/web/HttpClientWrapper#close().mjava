    /**
     * Safely clean up the HttpClient.
     */
    @Override
    public void close() throws IOException {
        closeResponse();
        if (m_httpClient != null) {
            m_httpClient.close();
        }
    }

