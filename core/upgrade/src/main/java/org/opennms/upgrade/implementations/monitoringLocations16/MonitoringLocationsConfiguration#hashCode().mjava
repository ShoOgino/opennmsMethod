    @Override
    public int hashCode() {
        final int prime = 383;
        int result = 1;
        result = prime * result + ((m_locations == null || m_locations.size() == 0) ? 0 : m_locations.hashCode());
        return result;
    }

