        private static boolean isWhitespace(char aChar) {
            switch(aChar) {
            case ' ':
            case '\t':
            case '\n':
            case '\r':
            case '\f':
                return true;
            default:
                return false;
            }
        }

