        public void put(final Properties properties) throws IOException {
            this.lock.lock();
            try {
                this.m_properties = properties;
                write();
            } finally {
                this.lock.unlock();
            }
        }

