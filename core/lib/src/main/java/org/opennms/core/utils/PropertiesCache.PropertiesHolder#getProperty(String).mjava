        public String getProperty(final String key) throws IOException {
            this.lock.lock();
            try {
                return get().getProperty(key);
            } finally {
                this.lock.unlock();
            }
            
        }

