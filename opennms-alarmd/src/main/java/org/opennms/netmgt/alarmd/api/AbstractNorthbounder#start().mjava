    @Override
    public void start() throws NorthbounderException {
        m_stopped = false;
        m_queue.init();
        m_thread = new Thread(this, getName()+"-Thread");
        m_thread.start();
    }

