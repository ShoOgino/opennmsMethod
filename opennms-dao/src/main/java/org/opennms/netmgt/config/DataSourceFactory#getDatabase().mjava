    /**
     * Return the database that was configured in the config file
     * 
     * @return the database that was configured in the config file
     */
    public Database getDatabase() {
        return m_database;
    }

