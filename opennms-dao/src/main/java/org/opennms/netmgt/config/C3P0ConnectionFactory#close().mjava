    /**
     * <p>close</p>
     *
     * @throws java.sql.SQLException if any.
     */
    public void close() throws SQLException {
        log().info("Closing c3p0 pool");
        m_pool.close();
    }

