    public int compareTo(ConfigRange r) {
        if (preceeds(r)) {
            // this is less than 
            return -1;
        } else if (follows(r)) {
            // this is greater than
            return 1;
        } else {
            // otherwise it overlaps
            return 0;
        }
    }

