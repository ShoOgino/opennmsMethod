    static private Map getCache(Class clazz) {
        Map cache = (Map)s_cacheCache.get(clazz);
        if (cache == null) {
            cache = new HashMap();
            s_cacheCache.put(clazz, cache);
        }
        return cache;
    }

