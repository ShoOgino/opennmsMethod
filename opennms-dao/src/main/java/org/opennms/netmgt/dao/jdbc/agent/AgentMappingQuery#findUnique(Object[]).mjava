    public OnmsNode findUnique(Object[] objs) {
        List nodes = execute(objs);
        if (nodes.size() > 0)
            return (OnmsNode) nodes.get(0);
        else
            return null;
    }

