    public OnmsEvent findUnique(Object[] objs) {
        List events = execute(objs);
        if (events.size() > 0)
            return (OnmsEvent) events.get(0);
        else
            return null;
    }

