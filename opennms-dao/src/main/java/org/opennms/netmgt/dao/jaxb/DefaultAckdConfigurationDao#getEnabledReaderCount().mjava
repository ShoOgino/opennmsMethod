    @Override
    public int getEnabledReaderCount() {
        return Long.valueOf(getConfig().getReaders().stream().filter(r -> {
            return r.getEnabled();
        }).count()).intValue();
    }

