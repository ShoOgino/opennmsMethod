    public FindCategoriesByNode(DataSource ds) {
        super(ds, "from categories, category_node where category_node.nodeId = ? and category_node.categoryId = categories.categoryId");
        declareParameter(new SqlParameter("nodeId", Types.INTEGER));
        compile();
    }

