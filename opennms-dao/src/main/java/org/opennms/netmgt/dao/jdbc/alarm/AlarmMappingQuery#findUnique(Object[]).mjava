    public OnmsAlarm findUnique(Object[] objs) {
        List alarms = execute(objs);
        if (alarms.size() > 0)
            return (OnmsAlarm) alarms.get(0);
        else
            return null;
    }

