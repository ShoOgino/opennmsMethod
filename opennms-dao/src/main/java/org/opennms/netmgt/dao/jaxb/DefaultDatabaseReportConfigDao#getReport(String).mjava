    private Report getReport(String name) {

        for(Report report : getReports()) {
            if (name.equals(report.getId())) {
                return report;
            }
        }

        return null;

    }

