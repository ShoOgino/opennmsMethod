    public DemandPoll findUnique(Object[] objs) {
        List poll = execute(objs);
        if (poll.size() > 0)
            return (DemandPoll) poll.get(0);
        else
            return null;
    }

