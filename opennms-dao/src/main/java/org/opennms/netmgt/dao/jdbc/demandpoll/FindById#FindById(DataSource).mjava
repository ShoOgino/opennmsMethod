    public FindById(DataSource ds) {
        super(ds, "FROM demandpolls as p WHERE p.id = ?");  // Should the clause include "FROM demandpolls as p"
        super.declareParameter(new SqlParameter("id", Types.INTEGER));
        compile();
    }

