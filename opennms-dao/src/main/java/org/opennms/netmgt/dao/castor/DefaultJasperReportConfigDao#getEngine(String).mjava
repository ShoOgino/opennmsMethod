    /** {@inheritDoc} */
    public String getEngine(String id) {
        Report report = getReport(id);
        if (report != null) {
            return report.getEngine();
        }
        return null;
    }

