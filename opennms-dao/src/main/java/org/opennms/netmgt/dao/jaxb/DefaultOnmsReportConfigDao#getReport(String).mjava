    private Report getReport(String id) {
        for (Report report : getConfig().getReports()) {
            if (id.equals(report.getId())) {
                return report;
            }
        }
        
        return null;
        
    }

