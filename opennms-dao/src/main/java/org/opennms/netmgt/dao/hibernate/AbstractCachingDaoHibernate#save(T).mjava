    /** {@inheritDoc} */
    @Override
    public void save(T entity) throws DataAccessException {
        super.save(entity);
        if (m_cache.get() != null) {
            m_cache.get().put(getKey(entity), entity);
        }
    }

