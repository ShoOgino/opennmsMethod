    public PollResult findUnique(Object[] objs) {
        List nodes = execute(objs);
        if (nodes.size() > 0)
            return (PollResult) nodes.get(0);
        else
            return null;
    }

