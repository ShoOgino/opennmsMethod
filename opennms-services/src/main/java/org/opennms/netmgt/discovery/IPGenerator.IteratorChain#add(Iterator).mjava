        /**
         * Adds an iterator to the chain
         * 
         * @param iter
         *            The iterator to add.
         */
        void add(Iterator iter) {
            m_iterators.add(iter);
        }

