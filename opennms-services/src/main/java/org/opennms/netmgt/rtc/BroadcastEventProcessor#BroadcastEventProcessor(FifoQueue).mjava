    /**
     * Constructor
     * 
     * @param updaterQ
     *            The queue where events of interest are added.
     */
    BroadcastEventProcessor(FifoQueue updaterQ) {
        m_updaterQ = updaterQ;
    }

