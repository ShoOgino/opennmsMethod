    /**
     * Constructor
     * 
     * @param updaterQ
     *            The queue where events of interest are added.
     */
    BroadcastEventProcessor(ExecutorService updaterQ) {
        m_updater = updaterQ;
    }

