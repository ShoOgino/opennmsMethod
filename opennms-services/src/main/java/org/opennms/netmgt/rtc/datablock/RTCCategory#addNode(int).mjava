    /**
     * Add to the nodes in this category
     *
     * @param nodeid
     *            the node ID to add
     */
    public void addNode(int nodeid) {
        if (!m_nodes.contains(nodeid)) {
            m_nodes.add(nodeid);
        }
    }

