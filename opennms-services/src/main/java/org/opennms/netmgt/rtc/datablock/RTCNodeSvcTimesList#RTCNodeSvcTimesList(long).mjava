    /**
     * Default constructor.
     */
    public RTCNodeSvcTimesList(long rollingWindow) {
        super();

        m_rollingWindow = rollingWindow;

        m_outTimeSince = -1;

        m_outTime = 0;
    }

