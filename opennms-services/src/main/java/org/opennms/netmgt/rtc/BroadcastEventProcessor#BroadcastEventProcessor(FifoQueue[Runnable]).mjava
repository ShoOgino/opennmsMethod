    /**
     * Constructor
     * 
     * @param updaterQ
     *            The queue where events of interest are added.
     */
    BroadcastEventProcessor(FifoQueue<Runnable> updaterQ) {
        m_updaterQ = updaterQ;
    }

