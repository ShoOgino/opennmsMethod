    public void deleteAll() {
        Iterator<PollableInterface> ite = m_members.values().iterator();
        while (ite.hasNext()) {
            ite.next().delete();
        }
        m_members.clear();
        m_node.clear();
    }

