    /**
     * <p>deleteAll</p>
     */
    public void deleteAll() {
        getContext().updatePollStatus("N");
        for (PollableInterface pi: m_members.values()) {
            pi.delete();
        }
        m_members.clear();
        m_node.clear();
    }

