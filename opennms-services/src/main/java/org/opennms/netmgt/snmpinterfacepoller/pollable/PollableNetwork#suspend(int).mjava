    /**
     * <p>suspend</p>
     *
     * @param nodeid a int.
     */
    public void suspend(int nodeid) {
        String ipprimary = getIp(nodeid);
        if (ipprimary != null) suspend(ipprimary);
    }

