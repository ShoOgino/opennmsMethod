    /**
     * <p>activate</p>
     *
     * @param nodeid a int.
     */
    public void activate(int nodeid) {
        String ipprimary = getIp(nodeid);
        if (ipprimary != null) activate(ipprimary);
    }

