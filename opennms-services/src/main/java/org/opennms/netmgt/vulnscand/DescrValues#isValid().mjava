    /**
     * <p>isValid</p>
     *
     * @return a boolean.
     */
    public boolean isValid() {
        if ((descr != null) && (severity > 0))
            return true;
        else
            return false;
    }

