    /**
     * <p>isValid</p>
     *
     * @return a boolean.
     */
    public boolean isValid() {
        if ((protocol != null))
            return true;
        else
            return false;
    }

