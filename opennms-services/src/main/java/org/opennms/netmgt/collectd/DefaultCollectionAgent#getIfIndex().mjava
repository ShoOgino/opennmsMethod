    private int getIfIndex() {
        if (m_ifIndex == -1) {
            m_ifIndex = (getIpInterface().getIfIndex() == null ? -1 : getIpInterface().getIfIndex().intValue());
        }
        return m_ifIndex;
        
    }

