    void logForceRescan() {
        
        if (log().isDebugEnabled()) {
        	log().debug("Forcing rescan.  IfAlias " + getNewIfAlias()
        					+ " for index " + getIndex()
        					+ " does not match DB value: "
                            + getCurrentIfAlias());
        }
    }

