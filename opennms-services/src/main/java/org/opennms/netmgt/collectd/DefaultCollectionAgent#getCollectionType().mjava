    private CollectionType getCollectionType() {
        if (m_collType == null) {
            m_collType = m_transactionalAgent.getCollectionType();
        }
        return m_collType;
        
    }

