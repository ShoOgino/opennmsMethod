    /**
     * <p>popShouldPersist</p>
     *
     * @return a boolean.
     */
    protected boolean popShouldPersist() {
        boolean top = top();
        m_stack.removeLast();
        return top;
    }

