    /**
     * Pauses the service if its currently running
     */
    public synchronized void pause() {
        if (!isRunning()) {
            return;
        }

        setStatus(PAUSE_PENDING);

        m_executor.pause();
        setStatus(PAUSED);
    }

