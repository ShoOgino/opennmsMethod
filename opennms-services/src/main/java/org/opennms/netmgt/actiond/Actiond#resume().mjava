    /**
     * Resumes the service if its currently paused
     */
    public synchronized void resume() {
        if (!isPaused()) {
            return;
        }

        setStatus(RESUME_PENDING);

        m_execution.resume();
        setStatus(RUNNING);
    }

