    /** {@inheritDoc} */
    public Package getPackage(final String name) {
        getReadLock().lock();
        try {
            for(final Package pkg : packages()) {
                if (pkg.getName().equals(name)) {
                    return pkg;
                }
            }
        } finally {
            getReadLock().unlock();
        }
        return null;
    }

