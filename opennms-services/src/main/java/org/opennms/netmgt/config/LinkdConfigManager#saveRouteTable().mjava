    /**
     * <p>saveRouteTable</p>
     *
     * @return a boolean.
     */
    protected boolean saveRouteTable() {
        if (m_config.hasSaveRouteTable()) return m_config.getSaveRouteTable();
        return true;
    }

