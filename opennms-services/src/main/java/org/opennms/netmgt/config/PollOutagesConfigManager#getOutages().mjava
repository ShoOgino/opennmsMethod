    /**
     * Return the outages configured.
     *
     * @return the outages configured
     */
    public Outage[] getOutages() {
        m_readLock.lock();
        try {
            return getConfig().getOutage();
        } finally {
            m_readLock.unlock();
        }
    }

