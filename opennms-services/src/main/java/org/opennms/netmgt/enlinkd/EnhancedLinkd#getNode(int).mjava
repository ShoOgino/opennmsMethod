    Node getNode(int nodeid) {
        synchronized (m_nodes) {
            for (Node node : m_nodes) {
                if (node.getNodeId() == nodeid)
                    return node;
            }
            return null;
        }
    }

