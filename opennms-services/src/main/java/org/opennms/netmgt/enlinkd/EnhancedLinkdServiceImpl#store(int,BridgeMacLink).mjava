    @Override
    public void store(int nodeId, BridgeMacLink link) {
        if (link == null)
            return;
        OnmsNode node = new OnmsNode();
        node.setId(nodeId);
        link.setNode(node);
        storeInMemory(link);
    }

