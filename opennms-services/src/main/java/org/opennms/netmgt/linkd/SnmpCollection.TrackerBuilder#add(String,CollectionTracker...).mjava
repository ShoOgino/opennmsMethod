    	public void add(String label, CollectionTracker... trackers) {
    		if (m_msg == null) {
    			m_msg = label;
    		} else {
    			m_msg = "/" + label;
    		}
    		
    		m_trackerList.addAll(Arrays.asList(trackers));
    	}

