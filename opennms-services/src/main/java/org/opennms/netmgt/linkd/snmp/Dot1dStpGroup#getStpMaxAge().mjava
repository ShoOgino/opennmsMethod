    /**
     * <p>getStpMaxAge</p>
     *
     * @return a int.
     */
    public int getStpMaxAge(){
    	Integer stpMaxAge = m_store.getInt32(STP_MAX_AGE);
    	if (stpMaxAge == null ) {
            return -1;
        }
    	return stpMaxAge;
    }

