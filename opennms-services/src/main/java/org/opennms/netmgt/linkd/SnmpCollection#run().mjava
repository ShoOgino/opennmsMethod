    @Override
    public void run() {
        if (suspendCollection) {
            sendSuspendedEvent();
            LOG.debug("run: address: {} Suspended!", str(m_address));
        } else {
            sendStartedEvent();
            runCollection();
            sendCompletedEvent();
        }
        runned = true;
    }

