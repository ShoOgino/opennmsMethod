    @Override
    public void run() {
        runned = true;
        if (suspendCollection) {
            sendSuspendedEvent();
            LOG.debug("run: address: {} Suspended!", str(m_address));
        } else {
            sendStartedEvent();
            runCollection();
            sendCompletedEvent();
        }
    }

