    /** {@inheritDoc} */
    @Override
    protected void onStop() {
        m_stopped = true;
        if (m_scheduler != null && m_scheduler.getStatus() == RUNNING) {
            m_scheduler.stop();
        }
    }

