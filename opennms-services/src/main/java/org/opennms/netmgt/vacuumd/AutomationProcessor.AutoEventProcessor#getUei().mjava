        String getUei() {
            if (hasEvent() && getAutoEvent().getUei().getContent().isPresent()) {
                return getAutoEvent().getUei().getContent().get();
            } else {
                return null;
            }
        }

