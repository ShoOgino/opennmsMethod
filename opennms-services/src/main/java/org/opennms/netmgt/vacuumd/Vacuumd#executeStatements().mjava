    /**
     * 
     */
    private void executeStatements() {
        if (!m_stopped) {
            String[] stmts = getVacuumdConfig().getStatements();
            for (String stmt : stmts) {
                runUpdate(stmt);
            }
        }
    }

