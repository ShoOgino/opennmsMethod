        /**
         * handle the next character, returns the next level
         */
        int next(final char ch) throws IOException {
            onTransition(ch);
            return m_level;
        }

