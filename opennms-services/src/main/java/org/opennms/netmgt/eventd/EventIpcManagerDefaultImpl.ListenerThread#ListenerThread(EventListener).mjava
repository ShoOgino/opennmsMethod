        /**
         * Constructor
         */
        ListenerThread(EventListener listener) {
            m_listener = listener;
            m_delegateThread = Executors.newFixedThreadPool(1);
        }

