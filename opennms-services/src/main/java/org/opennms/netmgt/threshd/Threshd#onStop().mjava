    /**
     * <p>onStop</p>
     */
    @Override
    protected void onStop() {
        if (m_scheduler != null) {
            m_scheduler.stop();
            m_scheduler = null;
        }
        m_receiver.close();
	}

