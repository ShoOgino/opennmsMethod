    /**
     * <p>getCreatesCompleted</p>
     *
     * @return a long.
     */
    @Override
    public long getCreatesCompleted() {
        if (getStatsStatus()) {
            return getRrdStrategy().getCreatesCompleted();
        } else {
            return 0;
        }
    }

