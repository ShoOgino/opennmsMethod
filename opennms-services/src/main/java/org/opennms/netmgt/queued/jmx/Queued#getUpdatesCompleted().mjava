    /**
     * <p>getUpdatesCompleted</p>
     *
     * @return a long.
     */
    @Override
    public long getUpdatesCompleted() {
        if (getStatsStatus()) {
            return getRrdStrategy().getUpdatesCompleted();
        } else {
            return 0;
        }
    }

