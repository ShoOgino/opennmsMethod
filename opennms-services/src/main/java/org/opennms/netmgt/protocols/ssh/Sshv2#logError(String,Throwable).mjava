    private Throwable logError(String message, Throwable error) {
        if (error == null) {
            error = new SshException(message);
        }
        setError(error);
        log().error(error);
        return error;
    }

