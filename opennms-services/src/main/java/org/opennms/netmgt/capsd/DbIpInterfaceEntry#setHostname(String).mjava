    /**
     * Sets the current hostname.
     * 
     * @param name
     *            The new hostname
     */
    public void setHostname(String name) {
        m_hostname = name;
        m_changed |= CHANGED_HOSTNAME;
    }

