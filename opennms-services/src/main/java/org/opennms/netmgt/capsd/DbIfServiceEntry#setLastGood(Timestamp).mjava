    /**
     * Sets the current last good poll time.
     * 
     * @param time
     *            The poll time.
     * 
     */
    void setLastGood(Timestamp time) {
        m_lastGood = time;
        m_changed |= CHANGED_LASTGOOD;
    }

