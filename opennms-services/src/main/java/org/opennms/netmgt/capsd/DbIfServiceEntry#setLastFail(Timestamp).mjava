    /**
     * Sets the current last fail poll time.
     * 
     * @param time
     *            The poll time.
     * 
     */
    void setLastFail(Timestamp time) {
        m_lastFail = time;
        m_changed |= CHANGED_LASTFAIL;
    }

