    boolean updateName(String newIfName) {
        if (newIfName == null || newIfName.equals(m_ifName)) {
            return false;
        } else {
            setName(newIfName);
            return true;
        }
    }

