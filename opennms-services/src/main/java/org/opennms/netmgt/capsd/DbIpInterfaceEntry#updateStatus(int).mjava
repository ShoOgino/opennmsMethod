    boolean updateStatus(int newStatus) {
        if (newStatus != -1 && newStatus != m_status) {
            setStatus(newStatus);
            return true;
        } else
            return false;
    }

