    protected void onStop() {
        if(m_scheduler!=null) {
            m_scheduler.stop();
        }
        if(m_receiver!=null) {
            m_receiver.close();
        }

        releaseServiceMonitors();
        m_scheduler = null;
	}

