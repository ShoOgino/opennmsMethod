    /**
     * <p>isPending</p>
     *
     * @return a boolean.
     */

    public boolean isPending() {
        if (m_pending.get()) {
            // still pending, check if we've timed out
            if (isTimedOut()) {
                m_pending.set(false);
            }
        }
        return m_pending.get();
    }

