    /**
     * <p>isPending</p>
     *
     * @return a boolean.
     */

    public synchronized boolean isPending() {
        if (m_pending) {
            // still pending, check if we've timed out
            if (isTimedOut()) {
                m_pending = false;
            }
        }
        return m_pending;
    }

