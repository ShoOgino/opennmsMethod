    /**
     * <p>processPending</p>
     */
    public synchronized void processPending() {
        for (Runnable r : m_pendingOutages) {
            r.run();
        }
        m_pendingOutages.clear();
        
    }

