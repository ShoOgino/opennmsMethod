    @Override
    protected void tearDown() throws Exception {
        MockUtil.println("------------ End Test " + getName() + " --------------------------");
        if (m_syslogd != null) {
            m_syslogd.stop();
        }
        super.tearDown();
    }

