    @Override
    public void onTearDown() throws Exception {
        if (m_trapd != null && m_doStop) {
            m_trapd.stop();
            m_trapd = null;
        }
        
        super.onTearDown();
    }

