    @Before
    public void setUp() throws Exception {
        if (firstTest) {
            setUpLogging();
        } else {
            setUpLinkdConfiguration();
        }
        firstTest = false;
    }

