    private void anticipateStart() {
        if (m_started) return;

        if (getRegisteredId() == null) {
            return;
        }

        anticipateGetMonitorId();


        m_started = true;

    }

