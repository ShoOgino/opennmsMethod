    protected void setUp() throws Exception {
        if (!shouldWeRun()) {
            return;
        }
        
        super.setUp();
        m_runSupers = true;

        m_ifSnmpc = new IfSnmpCollector(s_addr);
        runCollection();
    }

