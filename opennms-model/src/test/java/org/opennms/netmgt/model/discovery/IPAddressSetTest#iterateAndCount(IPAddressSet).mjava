    private static long iterateAndCount(IPAddressSet set) {
        int count = 0;
        Iterator<IPAddress> it = set.iterator();
        while(it.hasNext()) {
            it.next();
            count++;
        }
        return count;
    }

