    @Override
    public Set<?> unmarshal(final List<?> v) throws Exception {
        if (v == null || "".equals(v)) {
            return null;
        }
        return new LinkedHashSet<>(v);
    }

