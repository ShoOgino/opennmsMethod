    public void update(OnmsServiceType serviceType) {
        if (serviceType.getId() == null)
            throw new IllegalArgumentException("unable to udpate service that doesn't have an id");
        
        getJdbcTemplate().update("update service set serviceName = ? where serviceId = ? ", new Object[] { serviceType.getName(), serviceType.getId() });

    }

