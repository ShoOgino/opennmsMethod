    public OnmsServiceType load(Integer id) {
        OnmsServiceType svcTyp = get(id);
        if (svcTyp == null)
            throw new IllegalArgumentException("unable to load service type with id: "+id);
        return svcTyp;
    }

