    @Transient
    public boolean isUp() {
        if (m_scanReportPollResults != null) {
            for (final ScanReportPollResult result : m_scanReportPollResults) {
                if (!result.isUp()) {
                    return false;
                }
            }
        }
        return true;
    }

