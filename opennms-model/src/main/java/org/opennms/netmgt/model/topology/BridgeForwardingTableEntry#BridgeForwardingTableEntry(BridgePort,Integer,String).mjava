    public BridgeForwardingTableEntry(BridgePort bridgePort, Integer vlanId,
            String mac) {
        super();
        m_bridgePort = bridgePort;
        m_vlanId = vlanId;
        this.m_mac = mac;
    }

