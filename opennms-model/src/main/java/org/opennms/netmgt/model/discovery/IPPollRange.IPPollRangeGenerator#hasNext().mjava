        /**
         * <P>
         * If there are more elements left in the iteration then a value of true is returned. Else a
         * false value is returned.
         * </P>
         */
        @Override
        public boolean hasNext()
        {
            return hasMoreElements();
        }

