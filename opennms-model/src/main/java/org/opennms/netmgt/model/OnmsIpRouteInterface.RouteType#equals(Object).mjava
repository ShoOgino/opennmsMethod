        public boolean equals(Object o) {
            if (o instanceof RouteType) {
                return m_routeType == ((RouteType)o).m_routeType;
            }
            return false;
        }

