    @Transient
    @XmlTransient
    public String getResult() {
        if (m_status == null) {
            return "Unknown";
        }
        if (m_status.isUp()) {
            return "Success";
        } else {
            return "Failed: " + m_status.getReason();
        }
    }

