    @Override
    public int hashCode() {
        final int prime = 31;
        int result = 1;
        result = prime * result
                + ((m_protocol == null) ? 0 : m_protocol.hashCode());
        return result;
    }

