	@Override
	public int hashCode() {
		final int prime = 31;
		int result = 1;
		result = prime * result + m_nodeId;
		result = prime
				* result
				+ ((m_snmpprimaryaddr == null) ? 0 : m_snmpprimaryaddr
						.hashCode());
		return result;
	}

