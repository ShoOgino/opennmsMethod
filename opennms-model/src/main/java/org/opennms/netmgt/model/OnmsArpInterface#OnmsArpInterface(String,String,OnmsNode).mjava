    /** minimal constructor */
    public OnmsArpInterface(String ipAddr, String physAddr, OnmsNode node) {
        m_ipAddress = ipAddr;
        m_physAddr = physAddr;
        m_node = node;
        node.getArpInterfaces().add(this);
    }

