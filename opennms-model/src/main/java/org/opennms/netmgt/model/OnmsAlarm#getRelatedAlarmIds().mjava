    @Transient
    @XmlTransient
    public Set<Integer> getRelatedAlarmIds() {
        return getRelatedAlarmsForSituation().stream()
                .map(OnmsAlarm::getId)
                .collect(Collectors.toSet());
    }

