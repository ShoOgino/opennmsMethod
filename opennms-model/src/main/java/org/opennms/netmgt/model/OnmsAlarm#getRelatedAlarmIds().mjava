    @Transient
    @XmlTransient
    public Set<Integer> getRelatedAlarmIds() {
        return getRelatedAlarms().stream()
                .map(OnmsAlarm::getId)
                .collect(Collectors.toSet());
    }

