    public OnmsSnmpInterface(String ipaddr, Integer ifIndex, OnmsNode node) {
        m_ipAddr = ipaddr;
        m_ifIndex = ifIndex;
        m_node = node;
        node.getSnmpInterfaces().add(this);
    }

