    /**
     * <p>setMaps</p>
     *
     * @param maps a {@link java.util.List} object.
     */
    public void setMaps(List<OnmsMap> maps) {
        if (maps == this) return;
        clear();
        addAll(maps);
    }

