    public void stop() {
    	ThreadCategory.setPrefix(ImporterService.NAME);
        m_status = Fiber.STOP_PENDING;
        m_context.close();
        
        
        m_status = Fiber.STOPPED;
    }

