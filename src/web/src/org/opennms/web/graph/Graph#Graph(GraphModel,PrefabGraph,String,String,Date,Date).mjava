    public Graph(GraphModel model, PrefabGraph graph, String domain, String intf,
		Date start, Date end) {
	m_model = model;
	m_graph = graph;
	m_domain = domain;
	m_intf = intf;
	m_start = start;
	m_end = end;
    }

