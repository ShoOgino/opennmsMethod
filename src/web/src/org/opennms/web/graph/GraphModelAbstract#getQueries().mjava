    /**
     * Return a list of all known prefabricated graph definitions.
     */
    public PrefabGraph[] getQueries() {
        if (m_queries == null) {
	    initQueries();
        }

	return m_queries;
    }

