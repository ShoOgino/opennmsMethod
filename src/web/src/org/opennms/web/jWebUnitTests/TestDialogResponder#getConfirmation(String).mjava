    public boolean getConfirmation(String confirmationPrompt) {
        Boolean response = (Boolean)m_confirmResponses.get(confirmationPrompt);
        if (response == null)
            Assert.fail("Unanticipated confirmation '"+confirmationPrompt+"'");
        return response.booleanValue();
    }

