	public NewUser(	String fullName, 
                        String userId, 
				String passwd,
				String confirm, 
				String comments, 
				String email, 
				String numericSvc, 
				String numericPin, 
				String txtSvc,
				String txtPin, 
				String duty)
	{
		fullname = fullName;
		user = userId;
		passwd = passwd;
		confirmpasswd = confirm;
		comments = comments;
		email = email;
		numsvc = numericSvc;
		numpin = numericPin;
		txtsvc = txtSvc;
		txtpin = txtPin;
		dutysch = duty;
	}

