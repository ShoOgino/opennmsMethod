	/**
	*/
	public void setAutoAcknowledgeState(String state)
	{
		/*
		if (index < 0 || index > CONFIGURATION_STATES.length)
			throw new InvalidParameterException("The auto acknowlege state index("+index+") must be >= 0 and <= " + CONFIGURATION_STATES.length);
		*/
		m_autoAcknowledgeState = state;
	}

