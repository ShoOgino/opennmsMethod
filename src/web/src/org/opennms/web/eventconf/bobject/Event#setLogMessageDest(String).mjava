	/**
	*/
	public void setLogMessageDest(String dest)
	{
		/*
		if (index < 0 || index > LOGMSG_DEST_VALUES.length)
			throw new InvalidParameterException("The log message destination index("+index+") must be >= 0 and <= " + LOGMSG_DEST_VALUES.length);
		*/
		m_logMessageDest = dest;
	}

