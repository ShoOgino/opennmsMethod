    /** 
     * Clear all database URL and credential information so no more connections
     * can be requested. 
     */
    public void destroy() {
        this.url = null;
        this.username = null;
        this.password = null;
        this.properties = null;
    }

