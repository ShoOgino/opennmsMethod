		/**
		 * Constructs a new fiber thread and adds the instance
		 * to the pool.
		 *
		 * @param name	The name of the fiber.
		 */
		FiberThreadImpl(String name)
		{
			m_shutdown = false;
			m_status   = START_PENDING;
			m_delegateThread = new Thread(m_tGroup,this,name);
		}

