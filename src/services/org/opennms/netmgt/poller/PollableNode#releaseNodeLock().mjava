    public void releaseNodeLock() throws InterruptedException {
        synchronized (m_lock) {
            if (m_isLocked) {
                m_isLocked = false;
                m_lock.notifyAll();
            }
        }
    }

