    /**
     * @param resolution TODO
     * @param e
     */
    protected void resolveOutage(PollEvent resolution) {
        if (hasOpenOutage())
            m_cause = null;
        resetStatusChanged();
    }

