    /**
     * Constructor.
     */
    public PollableInterface(PollableNode node, InetAddress address) {
        super(PollStatus.STATUS_UNKNOWN);
        m_node = node;
        m_address = address;
    }

