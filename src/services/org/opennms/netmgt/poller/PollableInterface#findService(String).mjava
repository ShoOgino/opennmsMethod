    public PollableService findService(String svcName) {
        // Sanity check
        if (svcName == null)
            return null;

        return (PollableService) m_services.get(svcName);
    }

