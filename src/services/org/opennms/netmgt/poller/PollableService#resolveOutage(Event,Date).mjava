    protected void resolveOutage(Event e, Date date) {
        getOutage().resolve(e, date);
        getContext().resolveOutage(this, getOutage().getRegainEvent());
        setOutage(null);
        super.resolveOutage(e, date);
    }

