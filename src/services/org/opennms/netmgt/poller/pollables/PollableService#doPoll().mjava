    /**
     * @return
     */
    public PollStatus doPoll() {
        if (getContext().isNodeProcessingEnabled())
            return getParent().doPoll(this);
        else
            return poll();
    }

