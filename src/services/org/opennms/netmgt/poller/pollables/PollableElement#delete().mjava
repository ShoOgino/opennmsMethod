    public void delete() {
        synchronized (getTreeLock()) {
            m_deleted = true;
            if (m_parent != null) {
                getParent().deleteMember(this);
                getParent().recalculateStatus();
            }
        }
    }

