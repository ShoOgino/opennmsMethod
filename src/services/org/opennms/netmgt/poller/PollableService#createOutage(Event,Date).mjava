    protected void createOutage(Event e, Date date) {
        if (getOutage() == null) {
            setOutage(new PollOutage(this, e, date));
            getContext().openOutage(this, getOutage().getLostEvent());
        }
        super.createOutage(e, date);
    }

