    /**
     * @param element
     * @param e
     * @param date
     */
    public PollOutage(PollableElement element, Event lostEvent, Date lostDate) {
        m_element = element;
        m_lostEvent = lostEvent;
        m_lostDate = lostDate;
    }

