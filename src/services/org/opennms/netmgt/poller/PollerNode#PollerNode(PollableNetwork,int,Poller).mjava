    /**
     * Constructor.
     * @param network TODO
     */
    public PollerNode(PollableNetwork network, int nodeId, Poller poller) {
        super(PollStatus.STATUS_UP);
        m_poller = poller;

        m_nodeId = nodeId;

        m_lock = new Object();
        m_isLocked = false;

        m_isDeleted = false;
    }

