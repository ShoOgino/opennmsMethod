    /**
     * @param service
     * @return
     */
    public PollStatus doPoll(PollableElement elem) {
        if (getParent() == null)
            return poll(elem);
        else
            return getParent().doPoll(elem);
    }

