    private PollStatus pollRemainingEssentialServices(PollableService pSvc) {
        return (hasCriticalService() && getStatus() == PollStatus.STATUS_UP ? pollCriticalService(pSvc) : pollRemainingServices(pSvc));
    }

