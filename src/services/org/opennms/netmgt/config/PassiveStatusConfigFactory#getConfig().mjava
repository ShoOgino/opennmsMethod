    /**
     * Return the PassiveStatus configuration.
     * 
     * @return the PassiveStatus configuration
     */
    private synchronized PassiveStatusConfiguration getConfig() {
        return m_config;
    }

