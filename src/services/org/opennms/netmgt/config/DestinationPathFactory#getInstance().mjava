	/**
	 *
	 */
	public static synchronized DestinationPathFactory getInstance() 
	{
		if (!initialized)
			return null;
		
		if (instance == null)
		{
			instance = new DestinationPathFactory();
		}
		
		return instance;
	}

