    /**
     * Return the datasource configured for the database
     * 
     * @return the datasource configured for the database
     */
    public javax.sql.DataSource getDataSource() {
        return m_singleton;
    }

