	/**
	 * Returns the matching event configuration instance
	 * that is indexed by the passed UEI. This is an instance
	 * of the <code>Event</code> that was loaded from the 
	 * event configuration file.
	 *
	 * @return The loaded event matching the UEI
	 */
	public static Event getByUei(String uei)
	{
		return (Event)m_eventConf.getEventByUEI(uei);
	}

