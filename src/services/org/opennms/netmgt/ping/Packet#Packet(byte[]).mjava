	/**
	 * Creates a new discovery ping packet 
	 * from the passed buffer.
	 *
	 * @param buf	The buffer containing a refected ping packet.
	 */
	public Packet(byte[] buf)
	{
		m_pad = null; // ensure loadFromBuffer allocates pad
		loadFromBuffer(buf,0);
	}

