    public MockNotificationManager(NotifdConfigManager configManager, String mgrString) throws MarshalException, ValidationException {
        super(configManager);
        Reader reader = new StringReader(mgrString);
        m_mgrString = mgrString;
        parseXML(reader);
        
    }

