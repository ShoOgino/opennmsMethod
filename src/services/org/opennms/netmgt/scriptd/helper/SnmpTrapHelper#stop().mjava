    /**
     * Stops the SnmpTrapHelper. If there is a valid SnmpTrapSession, that trap
     * session is stopped.
     */
    public void stop() {
        if (m_trapSession != null) {
            m_trapSession.close();
        }
    }

