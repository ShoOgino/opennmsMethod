    /**
     * 
     */
    public static InstanceTracker get(SnmpObjId base, String instances) {
        if (SnmpCollectionTracker.COLUMN.equals(instances)) {
            return new ColumnInstanceTracker(base);
        } else {
            return new SpecificInstanceTracker(base, instances);
        }
    }

