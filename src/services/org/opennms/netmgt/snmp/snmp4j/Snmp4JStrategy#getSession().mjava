    private static Snmp getSession() throws IOException {
        if (sm_session == null) {
            sm_session = new Snmp(new DefaultUdpTransportMapping());
        }
        return sm_session;
    }

