    public static PDU createPDU(int version) {
        PDU request;
        if (version == SnmpConstants.version3)
            request = new ScopedPDU();
        else
            request = new PDU();
        return request;
    }

