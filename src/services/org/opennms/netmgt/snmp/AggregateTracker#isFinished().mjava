    public boolean isFinished() {
        for(int i = 0; i < m_children.length; i++) {
            if (!m_children[i].isFinished())
                return false;
        }
        return true;
    }

