    /**
     * @param nodeID
     * @param ipAddr
     * @param serviceID
     * @return
     */
    BasicService getService(long nodeID, String ipAddr, long serviceID) {
        return new BasicService(getInterface(nodeID, ipAddr), serviceID);
    }

