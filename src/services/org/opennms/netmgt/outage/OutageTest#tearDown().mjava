    protected void tearDown() throws Exception {
        if (m_started) {
            m_outageMgr.stop();
        }
        sleep(100);
        assertTrue(MockUtil.noWarningsOrHigherLogged());
        m_db.drop();
    }

