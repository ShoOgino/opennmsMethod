    /**
     * Closes the client's socket connection to the DHCP daemon.
     * 
     * @throws IOException
     *             if the socket close() method fails.
     */
    public void close() {
        try {
            m_connection.close();
        } catch (Throwable ex) {
        }
    }

