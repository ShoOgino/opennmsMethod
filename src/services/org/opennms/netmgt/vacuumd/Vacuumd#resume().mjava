    /* (non-Javadoc)
     * @see org.opennms.core.fiber.PausableFiber#resume()
     */
    public void resume() {
        if (m_status != PAUSED)
            return;
        
        m_thread = new Thread(this, "Vacuumd-Thread");
        m_thread.start();
    }

