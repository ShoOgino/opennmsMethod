    /* (non-Javadoc)
     * @see org.opennms.core.fiber.PausableFiber#pause()
     */
    public void pause() {
        if (m_status != RUNNING)
            return;

        m_status = PAUSE_PENDING;
        m_stopped = true;
        m_status = PAUSED;
    }

