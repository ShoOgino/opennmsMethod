		/**
		 * Returns the extends status of the thread. This
		 * will always be one of two values: POLLING or
		 * IDLE.
		 *
		 * @return The current polling status.
		 *
		 */
		public synchronized int getExtendedStatus()
		{
			return m_xstatus;
		}

