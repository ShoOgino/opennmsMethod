		/**
		 * Returns true if this thread is currently polling the
		 * passed IP Address.
		 *
		 * @param addr	The address to check.
		 *
		 * @return True if the thread is currently targeting the
		 * 	passed address.
		 */
		public synchronized boolean isPinging(InetAddress addr)
		{
			return m_target.equals(addr);
		}

