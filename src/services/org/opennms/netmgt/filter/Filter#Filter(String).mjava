	/**
	 * Constructs a new filter with the pre-defined rule
	 * as the comparision method.
	 *
	 * @param rule	The filter rule.
	 *
	 * @exception FilterParseException, if a rule is syntactically incorrect or
         *            failed in executing the SQL statement.
	 */
	public Filter(String rule)
		throws FilterParseException
	{
		m_parseTree = null;
		parseRule(rule);
	}

