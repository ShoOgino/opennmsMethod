    /**
     * Required method for TestCase
     */
    protected void setUp() throws Exception {
        super.setUp();
        if (m_plugin == null) {
            m_plugin = new SnmpPlugin();
        }
        m_runSupers = false;
    }

