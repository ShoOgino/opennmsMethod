    boolean updateAlias(String newIfAlias) {
        if (newIfAlias == null || newIfAlias.equals(m_ifAlias))
            return false;
        else {
            setAlias(newIfAlias);
            return true;
        }
    }

