	/**
	 * Default constructor. Constructs an empty 
	 * node entry with no data. The default distributed
	 * poller name is used internally.
	 *
	 */
	private DbNodeEntry()
	{
		this(DEFAULT_DP_NAME);
	}

