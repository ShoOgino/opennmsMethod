    /**
     * Sets the last poll time.
     * 
     * @param time
     *            The last poll time.
     * 
     */
    void setLastPoll(Timestamp time) {
        m_lastPoll = time;
        m_changed |= CHANGED_POLLTIME;
    }

