    /**
     * @param protocol
     * @param defaultPort
     * @param defaultTimeout
     * @param defaultRetry
     */
    public ImapPlugin() {
        super(PROTOCOL_NAME, DEFAULT_PORT, DEFAULT_TIMEOUT, DEFAULT_RETRY);
    }

