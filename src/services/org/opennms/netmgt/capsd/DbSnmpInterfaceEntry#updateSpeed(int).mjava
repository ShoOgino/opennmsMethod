    boolean updateSpeed(int newIfSpeed) {
        if (newIfSpeed == -1 || newIfSpeed == m_ifSpeed)
            return false;
        else {
            setSpeed(newIfSpeed);
            return true;
        }
    }

