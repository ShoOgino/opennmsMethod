	/**
	 * This method inspects the provided netBIOS name
	 * for control characters (chars w/ decimal value
	 * less than 20/<SPACE>
	 * 
	 * @param nbName  NetBIOS name to check
	 *
	 * @return true if string contains control chars,
	 *  	   false otherwise.
	 */
	boolean containsCtrlChars(String nbName)
	{
		byte[] bytes = nbName.getBytes();
		for (int i=0; i<bytes.length; i++)
		{
			if (bytes[i] < 20)
				return true;
		}
		
		return false;
	}

