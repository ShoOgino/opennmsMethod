    boolean updateIfIndex(int newIfIndex) {
        if (newIfIndex != -1 && newIfIndex != m_ifIndex) {
            setIfIndex(newIfIndex);
            return true;
        }
        return false;
    }

