	/**
	 * <P>Returns the list of entry maps
	 * that can be used to access all the
	 * information about the interface extensions table.</P>
	 *
	 * @return The list of ifXTableEntry maps.
	 */
	public List getEntries()
	{
		return m_entries;
	}

