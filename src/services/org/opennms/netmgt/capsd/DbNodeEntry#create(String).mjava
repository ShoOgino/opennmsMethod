	/**
	 * Creates a new entry. The entry is created in memory, but
	 * is not written to the database until the first call
	 * to <code>store</code>.
	 *
	 * @param poller	The name of the distributed poller to use.
	 *
	 * @return A new node record.
	 */
	static DbNodeEntry create(String poller)
	{
		if(poller == null)
			poller = DEFAULT_DP_NAME;

		return new DbNodeEntry(poller);
	}

