    public final void testGetIfAlias() {
        if(m_run) {

            if(!m_hasRun)
                m_ifSnmpc.run();
            
            SnmpOctetString ifAlias = m_ifSnmpc.getIfAlias(1);
            assertNotNull(ifAlias);
        }
    }

