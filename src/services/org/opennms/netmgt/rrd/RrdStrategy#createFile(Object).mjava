    /**
     * Creates the round robin database defined by the supplied definition.
     * 
     * @param rrdDef
     *     an round robin database definition created using the createDefinition call.
     * @throws Exception if an error occurs create the file
     */
    void createFile(Object rrdDef) throws Exception;

