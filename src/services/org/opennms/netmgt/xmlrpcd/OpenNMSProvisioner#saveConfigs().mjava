    private void saveConfigs() {
        try {
            m_capsdConfig.save();
            syncServices();
            m_pollerConfig.save();
        } catch (Exception e) {
            throw new RuntimeException("Error saving poller or capsd configuration", e);
        }
    }

