    public void init() {
        if (!m_initialized) {
            checkPreRequisites();
            createMessageSelectorAndSubscribe();
            m_statusTable = new HashMap();
            m_initialized = true;
            setStatus(START_PENDING);
        }
    }

