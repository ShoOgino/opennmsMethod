	/**
	 * <P>Creates a default instance of the SNMPCollector
	 *  entry map. The map represents a singular
	 *  instance from the MibObject. Each column in
	 *  the table for the loaded instance may be retrieved
	 *  through its OID from the MIBObject.</P>
	 *
	 * <P>The initial table is constructed with zero
	 * elements in the map.</P>
	 */
	public SNMPCollectorEntry( )
	{
		super();
		m_objList = null;
	}

