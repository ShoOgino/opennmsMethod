    private void signal() {
        // notify anyone waiting on this
        // particular object
        //
        synchronized (this) {
            this.notifyAll();
        }
        if (m_signal != null) {
            m_signal.signalAll();
        }
    }

