	int getSavedIfCount() {
		int savedIfCount = -1;
		Integer tmp = (Integer) getAttribute(SnmpCollector.INTERFACE_COUNT_KEY);
		if (tmp != null) {
			savedIfCount = tmp.intValue();
		}
		return savedIfCount;
	}

