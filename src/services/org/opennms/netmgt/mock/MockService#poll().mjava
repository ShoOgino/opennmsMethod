    // test
    public int poll() {
        m_pollCount++;

        Iterator it = m_triggers.iterator();
        while (it.hasNext()) {
            PollAnticipator trigger = (PollAnticipator) it.next();
            trigger.poll(this);
        }

        return getPollStatus();

    }

