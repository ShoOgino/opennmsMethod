    public void startServer() {
        m_server = new Server();
        m_server.setDatabasePath(0, "mem:test");
        synchronized(m_server) {
            m_server.start();
        }
        
    }

