	/**
	 * Returns the number of bytes required to read/write
	 * an icmp header. A header is composed of a fixed size
	 * number of byte. This is not expected to change, but 
	 * this method allows derived classes to query the number
	 * of bytes for reading/writing an icmp header. Thus should
	 * the standard ever change, the derived class should be able
	 * to dynamically handle the change.
	 *
	 * @return The number of bytes required to read/write an icmp header.
	 *
	 */
	public static int getNetworkSize( )
	{
		return 8;
	}

