	/**
	 * Sets the header's 16-bit user defined
	 * identity value.
	 *
	 * @param identity The header's new identity.
	 *
	 */
	public final void setIdentity(short identity)
	{
		m_ident = identity;
	}

