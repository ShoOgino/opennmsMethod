    /**
     * Converts a byte to a short.
     * 
     * @param b
     *            The byte to convert.
     * 
     * @return The converted byte.
     * 
     */
    protected static short byteToShort(byte b) {
        short s = (short) b;
        if (s < 0)
            s += 256;
        return s;
    }

