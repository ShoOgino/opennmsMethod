	/**
	 * Gets the next global identifier
	 * and sets the value in the object. In
	 * addition the new sequence identifier
	 * is returned.
	 *
	 * @return The new 16-bit sequence identifier.
	 *
	 */
	public final short setNextSequenceId( )
	{
		m_sequence = nextSequenceId();
		return m_sequence;
	}

