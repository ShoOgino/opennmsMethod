	/**
	 * Constructs an ICMP header with the specified
	 * header fields.
	 *
	 * @param type		The 8-bit ICMP type.
	 * @param code		The 8-bit ICMP code.
	 * @param checksum	The 16-bit checksum header.
	 * @param identity	The 16-bit identity (user).
	 * @param sequence	The 16-bit sequence id.
	 *
	 */
	public ICMPHeader(byte	type,
			  byte	code,
			  short	checksum,
			  short identity,
			  short sequence)
	{
		m_type		= type;
		m_code		= code;
		m_checksum	= checksum;
		m_ident		= identity;
		m_sequence	= sequence;
	}

