	/**
	 * Returns the current checksum value that has
	 * been computed for the object.
	 *
	 * @return The 16-bit ones compliment checksum
	 *
	 */
	public short getChecksum( )
	{
		if(m_finalized == false)
			finalizeSum( );

		return (short)((~m_cksum) & 0xffff);
	}

