	/** 
	 * Adds the specified 16-bit value to the checksum
	 * total. 
	 *
	 * @param s The 16-bit value to add
	 *
	 */
	public void add(short s)
	{
		m_cksum += shortToInt(s);
		m_finalized = false;
	}

