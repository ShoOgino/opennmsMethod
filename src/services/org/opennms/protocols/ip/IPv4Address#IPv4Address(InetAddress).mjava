	/** 
	 * Creates a new IPv4Address from the passed InetAddress
	 * object.
	 *
	 * @param addr	The Internet Address containing the IPv4 address.
	 *
	 */
	public IPv4Address(InetAddress addr)
	{
		m_addr = dup(addr.getAddress());
	}

