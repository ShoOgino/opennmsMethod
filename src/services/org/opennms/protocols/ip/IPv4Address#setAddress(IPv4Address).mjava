	/** 
	 * Sets the current address based
	 * upon the value of the passed object.
	 *
	 * @param second	The new address.
	 */
	public void setAddress(IPv4Address second)
	{
		m_addr = dup(second.m_addr);
	}

