	/**
	 * Adds the specified 32-bit value to the checksum.
	 * The 32-bit value is broken into two 16-bit values
	 * for the purpose of computing the checksum.
	 *
	 * @param i The 32-bit value.
	 *
	 */
	public void add(int i)
	{
		m_cksum += ((i >> 16) & 0xffff);
		m_cksum += (i & 0xffff);
		m_finalized = false;
	}

