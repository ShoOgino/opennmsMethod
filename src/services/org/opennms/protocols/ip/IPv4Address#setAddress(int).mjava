	/**
	 * Sets the address object based on the 32-bit passed
	 * value. The 32-bit integer is split into four eight bit values
	 * that represent the IPv4 address.
	 *
	 * @param ipv4Addr	The 32-bit IP address.
	 *
	 */
	public void setAddress(int ipv4Addr)
	{
		m_addr = new byte[4];
		m_addr[0] = (byte)(ipv4Addr >>> 24);
		m_addr[1] = (byte)((ipv4Addr >> 16) & 0xff);
		m_addr[2] = (byte)((ipv4Addr >>  8) & 0xff);
		m_addr[3] = (byte)(ipv4Addr & 0xff);
	}

