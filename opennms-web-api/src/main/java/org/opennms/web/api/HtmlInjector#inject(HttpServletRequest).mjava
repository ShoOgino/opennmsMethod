    /**
     * Inject content into the current page.
     *
     * @param request the current HTTP request
     * @return HTML content to inject, or null
     */
    String inject(HttpServletRequest request) throws Exception;

