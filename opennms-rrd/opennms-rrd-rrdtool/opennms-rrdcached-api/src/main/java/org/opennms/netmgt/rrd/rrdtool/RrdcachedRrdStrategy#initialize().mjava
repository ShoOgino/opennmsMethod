    public synchronized void initialize() {
        try {
            m_rrdcached = new Rrdcached(InetAddress.getByName("localhost"), 42217);
            initialized = true;
        } catch (IOException e) {
        }
    }

