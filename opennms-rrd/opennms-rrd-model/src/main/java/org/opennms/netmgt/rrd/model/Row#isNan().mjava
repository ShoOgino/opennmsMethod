    /**
     * Checks if is all the values are NaN.
     *
     * @return true, if all the values are NaN.
     */
    @XmlTransient
    public boolean isNan() {
        for (Double v : values) {
            if (!v.isNaN()) {
                return false;
            }
        }
        return true;
    }

