        public void addData(String owner, String data) {
            m_messages.addMessage(RrdMessage.newBuilder()
                    .setPath(m_filename)
                    .setOwner(owner)
                    .setData(data)
            );
        }

