        public RrdDefinition(
                String creator,
                String directory,
                String rrdName,
                int step
        ) {
            m_creator = creator;
            m_directory = directory;
            m_rrdName = rrdName;
            m_step = step;
        }

