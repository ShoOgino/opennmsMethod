    /**
     * Default constructor, intializes the members
     */
    public Command() {
        m_arguments = new ArrayList();
        m_types = new ArrayList();
        m_useStream = false;
    }

