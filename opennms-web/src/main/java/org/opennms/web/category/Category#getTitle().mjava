    /** Returns the category comment if there is one, otherwise, its name. */
    public String getTitle() {
        if (getComment() != null) {
            return getComment();
        } else {
            return getName();
        }
    }

