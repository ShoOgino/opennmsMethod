    @Override
    public T load(K id) {
        T retval = m_entries.get(id);
        LOG.debug("load({}: {})", retval == null ? "null" : retval.getClass().getSimpleName(), id);
        return retval;
    }

