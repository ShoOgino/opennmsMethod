    /**
     * Resets the event listeners and resets the event anticipator.
     */
    public void reset() {
        m_listeners = new ArrayList<ListenerKeeper>();
        m_anticipator.reset();
    }

