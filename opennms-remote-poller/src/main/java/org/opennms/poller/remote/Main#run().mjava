    private void run() {
        
        parseArguments();
        
        createAppContext();
        
        registerShutDownHook();

        if (!m_frontEnd.isRegistered()) {
            m_frontEnd.register(m_locationName);
        }    
                
        
    }

